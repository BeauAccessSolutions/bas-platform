# Platform Tracker

Living status board for the Beau Access Solutions accessibility-app platform. Update as
things move — this is the single place to see where everything stands.

**Last updated:** 2026-07-24 (**Platform — multi-role matrix run against all four member apps; one blocker fixed in each of three, five PRs open, plus a four-app visual UX review received.** Completing the 2026-07-23 sweep: **KindredAccess** — F1 (a chat message could be destroyed silently: fire-and-forget `socket.send` on a half-open socket + composer cleared before ack) fixed in **[PR #20](https://github.com/BeauAccessSolutions/kindredaccess/pull/20)** with a client-side pending/failed/retry state machine, idempotent on `(sender, client_msg_id)`, +2 self-caught bugs (assertive announce silently downgraded to polite; retry dropped focus to `<body>`); F2 (no audit model anywhere; staff bypass the media proxy to view any photo/chat image, logging only denials) fixed in **[PR #21](https://github.com/BeauAccessSolutions/kindredaccess/pull/21)** — new `StaffMediaAccess`, append-only at three levels, only privileged reads logged. **CIT** — audit found it's the odd one out (single-user PHI, **no admin surface**, so dual-hat/impersonation/`AUDIT-01`-role are genuinely N/A); C1 (nothing protected a partial entry — no guard, no autosave, no draft; App Router nav doesn't fire `beforeunload`) fixed in **[PR #72](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/72)** — `localStorage` drafts in the shared `ApiForm`, PHI envelope (expiry, clear-on-save, clear-on-delete), +3 self-caught bugs (ref detached before effect cleanup so the unmount flush read null; conditionally-inserted live region; a two-match test query). **Access Atlas** — **cleanest of the four, nothing to fix**; independently implements `moderation_audit` append-only *at the database level* (service_role INSERT+SELECT, no UPDATE/DELETE — stronger than the KA F2 fix), audit rows as plain uuids not FKs so a takedown can't cascade away its own evidence, redaction removes storage objects before nulling the URL; one finding (30-day sessions at the AAL1 ceiling), one out-of-matrix note (no rate limiting on public writes). Audits: [BN](docs/testing/bn-blocker-audit-2026-07-23.md) · [KA](docs/testing/kindredaccess-blocker-audit-2026-07-23.md) · [CIT](docs/testing/cit-blocker-audit-2026-07-23.md) · [AA](docs/testing/access-atlas-blocker-audit-2026-07-23.md). **Cross-app finding → matrix updated:** P1 (session timeout) **cannot be one portfolio number** — four apps, four defensible AALs (BN AAL2 15m/12h · KA AAL2-contested 2wk · CIT AAL2-single-user 24h · AA AAL1 30d); `P1_session_timeouts` now carries a `per_app` block, the portfolio rule being the *derivation* (AAL from the data → timeout from the AAL → tightening gated on drafts-survive-expiry), still `proposed`. **Five PRs open, all green, all stacked/blocked on review the solo owner can't self-supply:** BN [#64](https://github.com/BeauAccessSolutions/benefits_navigator/pull/64)→[#62](https://github.com/BeauAccessSolutions/benefits_navigator/pull/62), KA [#20](https://github.com/BeauAccessSolutions/kindredaccess/pull/20)→[#21](https://github.com/BeauAccessSolutions/kindredaccess/pull/21), CIT [#72](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/72). Migrations pending on deploy: KA `0045`+`0046`, CIT none (client-only). **Four-app visual UX review received 2026-07-24** (screenshots-only, user + chat Claude) → captured + code-verified in [docs/testing/four-app-visual-review-2026-07-24.md](docs/testing/four-app-visual-review-2026-07-24.md). Its dominant theme (happy paths built, failure paths empty) independently matches the matrix; PRs #20/#72 already close part of it. **Two of its Tier-1 "blocking" fixes target the wrong framework** — CIT is Next.js not Django (the `SESSION_COOKIE_SAMESITE`/allauth hypothesis is off-target; the real lead is Keycloak token-verification: issuer/audience/clock, matching the recent OIDC-diagnostics debugging and the known "client IDs aren't uniformly named" risk), and Access Atlas is Astro not SvelteKit (the SvelteKit `ORIGIN` env-var fix does nothing; Astro's `security.checkOrigin` + forwarded-host derivation is the target). Review's structural §4 flags the decision gating the most work: **will VSOs enter veteran case data into BN, or is it a reference tool?** — reference→ship after UI fixes; system-of-record→ POA-scoped ReBAC + org tenancy + §7332 segmentation become launch requirements. §7332 (VA's stricter 42-CFR-Part-2 analog) is new and should join the [counsel briefs](docs/legal/bn-data-posture-counsel-brief.md). **Two review Tier-1 blockers fixed, correctly-targeted after the stack corrections:** **Access Atlas CSRF** [#31](https://github.com/BeauAccessSolutions/access-atlas/pull/31) — every prod form POST 403'd ("Cross-site POST forbidden"); traced through `@astrojs/node@9.5.5`/`astro@5.18.2` internals: empty `security.allowedDomains` → Astro refuses the DO-App-Platform-forwarded host, falls back to `localhost`, computes `https://localhost` ≠ browser origin. Fixed via build-time `SITE_ORIGINS` (hostname-only patterns — a `protocol` field routes proto-validation through a dummy `example.com` and silently reintroduces the bug). Verified against the built server with forged proxy headers: unset→403, set+real→303, set+attacker→403. **CIT sign-in** [#73](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/73) — platform-OIDC path (`/api/auth/session`→`verifyIdentityToken`) was blind by design (deterministic 401, nothing logged). Now logs jose error code + failing claim NAME (PHI-free; decoded token never touched), so the next failed login names its cause — prime suspect `KEYCLOAK_CLIENT_ID` `aud`/`azp` mismatch per the non-uniform-client-id risk. Doesn't change auth; root config lives in the deploy/Keycloak registration. **Screenshots (`~/Desktop/SCREENSHOTS`) reviewed — corroborate the review on screen:** KA timezone bug visible (msg `1:06 PM` vs status bar `9:06`, UTC unconverted), leaked Jinja comment as body text, BN unstyled allauth across signup + socialaccount pages + raw markdown; CIT sign-in error screen NOT among the 9 viewed (why #73 is make-it-diagnosable not root-cause). **Open PR count now 7:** BN [#64](https://github.com/BeauAccessSolutions/benefits_navigator/pull/64)→[#62](https://github.com/BeauAccessSolutions/benefits_navigator/pull/62), KA [#20](https://github.com/BeauAccessSolutions/kindredaccess/pull/20)→[#21](https://github.com/BeauAccessSolutions/kindredaccess/pull/21), CIT [#72](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/72)+[#73](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/73), AA [#31](https://github.com/BeauAccessSolutions/access-atlas/pull/31). Full review capture: [docs/testing/four-app-visual-review-2026-07-24.md](docs/testing/four-app-visual-review-2026-07-24.md). **X1 safe-area inset (content under the iOS status bar) fixed across ALL FOUR apps** — Atlas [#32](https://github.com/BeauAccessSolutions/access-atlas/pull/32) · KA [#22](https://github.com/BeauAccessSolutions/kindredaccess/pull/22) · CIT [#74](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/74) · BN [#71](https://github.com/BeauAccessSolutions/benefits_navigator/pull/71). Review's guessed cause was incomplete two ways: (a) `env(safe-area-inset-*)` is 0 unless `viewport-fit=cover` is set — so KA's *existing* bottom-inset rules were already dead no-ops; (b) padding the scroll container can't fix it (padding scrolls away) — only a FIXED backdrop stays over the status-bar zone. Diverged per app: Atlas/KA/BN non-sticky headers → fixed strip in brand color; CIT header already sticky → pad the header (a strip would clip it). Caught mid-impl: BN's CSP (`style-src 'self'`, no unsafe-inline) forced a linked stylesheet not inline; KA's allows inline. All gated on env()=0 → inert on desktop/non-notched. Builds/tests pass; **mechanism verified but on-device (real notch) visual NOT — each PR says so.** **Open PR total now 11:** audit sweep 5 (BN #62/#64, KA #20/#21, CIT #72) + Atlas CSRF #31 + CIT OIDC #73 + safe-area ×4 (#32/#22/#74/#71). BN #71 shares the inherited #64 red; other three on green mains.) · 2026-07-23 (**Platform — multi-role test matrix built; first run against Benefits Navigator found 3 blockers and a red `main`.** A research pass on logic traps / state failures / access-control errors in multi-role apps was converted from prose into an executable matrix: [`docs/testing/multi-role-test-matrix.yaml`](docs/testing/multi-role-test-matrix.yaml) (**77 tests, 11 suites, 38 ship-blocking, 54 negative**) plus a runner ([`scripts/testmatrix.py`](scripts/testmatrix.py): `validate` / `list` / `md` / `csv` / `issues --repo … [--execute]` / `gate`). Every test states its **pass and fail conditions separately** — a test whose only criterion is "it works" gets marked green by whoever runs it — and constants are `{P1.idle_default}`-style placeholders resolved from a parameter block, so a timeout changes in one place. **NIST verified against the primary source** (`pages.nist.gov/800-63-4/sp800-63b/`): AAL2 is ≤1hr idle / ≤24hr reauth (the ≤30min/≤12hr figures still in circulation are 800-63B-3), and two things the synthesis flattened — AAL3's overall timeout is a **SHALL** ≤12hr while AAL2's is a SHALL *to establish one* with a SHOULD on length, and in federation "the RP **SHALL** be authoritative as to whether the reauthentication requirements have been met" (binds each app, not Keycloak — same split as [INVARIANTS](INVARIANTS.md) §1). Five product parameters (P1 timeouts 15m/12h · P2 small-cell 11 · P3 delegation 90d/no-re-delegation · P4 offline 24h-without-auth-refresh · P5 age of majority) carry written rationale and sit at `proposed`; `testmatrix.py gate` **exits non-zero until each is adopted**, and P5 is `pending_counsel` → [`docs/legal/minors-delegation-counsel-brief.md`](docs/legal/minors-delegation-counsel-brief.md) (5 questions; the one flagged as BAS-specific: several apps serve adults with continuing guardianship past 18, so a clean "revoke at majority" rule may be actively wrong here — a data-model question, not a config value). **Matrix is portfolio-level; only BN has been run against it** — KindredAccess, CIT, and Access Atlas are untouched. **BN audit** ([`docs/testing/bn-blocker-audit-2026-07-23.md`](docs/testing/bn-blocker-audit-2026-07-23.md), static read at `25b1a40`, not-run tests listed as not-run rather than passed): 4 notable passes — `get_scoped_case_or_404` is the sole sanctioned case-by-pk lookup at all ~12 sites and raises `Http404` not `PermissionDenied` (so `AUTHZ-01` and `AUTHZ-12` pass together), org selection re-validates against live memberships every request (`SESS-06` by construction), bulk export is admin-only + rate-limited + ops-alerted + email-stripped. 3 blockers filed: [#59](https://github.com/BeauAccessSolutions/benefits_navigator/issues/59) `AUTHZ-05` — `restrict_caseworker_visibility` is `default=False`, so least-privilege caseload scoping is **dark by default** and every caseworker sees every case in the org (mechanism is well-built, just off); [#60](https://github.com/BeauAccessSolutions/benefits_navigator/issues/60) `REV-01` — **no idempotency anywhere in the repo**, so `case_create`/`invite_veteran`/`add_case_note` are double-submittable (duplicate *invitations* are outbound email to a veteran); [#61](https://github.com/BeauAccessSolutions/benefits_navigator/issues/61) `AUDIT-01` — no `role`/`mode` on any audit entry, so a dual-hat access can't be attributed to the hat. 2 fixed in **[PR #62](https://github.com/BeauAccessSolutions/benefits_navigator/pull/62)**: `AuditLogAdmin` let **superusers delete audit history** (add/change were blocked, delete wasn't), and the three bulk-export call sites bypassed `AuditLog.log()` to store `REMOTE_ADDR` — the App Platform load balancer, not the caller — on exactly the events an incident responder needs; `tests/test_audit_integrity.py` covers both and both fail against the prior code. 🔴 **Found along the way: `main` had been RED since `c10776c` (#58).** That PR was correct — it moved staticfiles into `STORAGES` so whitenoise's manifest backend actually applies — but `{% static %}` then resolves through a `staticfiles.json` that a gitignored `STATIC_ROOT` doesn't have, so every page-rendering test 500s with "Missing staticfiles manifest entry"; with `pytest -x`, **40 of 1120 tests ran** and every PR opened off `main` was unverifiable. Fixed in **[PR #64](https://github.com/BeauAccessSolutions/benefits_navigator/pull/64)** with a session-scoped autouse conftest fixture (fixes CI, both workflows, and local fresh clones in one place; deliberately does *not* swap the backend under tests, so #58's own guard still passes). **Both PRs green, both stacked (#62 → #64 → `main`), both awaiting merge** — branch protection requires a review the solo owner can't self-supply, and `gh pr merge --admin` is not available to this session. Note a peer worktree on `claude/fix-staticfiles-manifest` (empty) — a second session started the same bug.) · 2026-07-23 (🚀 **KindredAccess — child-safety remediation MERGED + DEPLOYED TO PRODUCTION (`b5b6036` live on the droplet).** A second 5-specialist re-review of the 07-21 pass verified all five prior blockers fixed at HEAD and surfaced residual gaps in the evidence hold itself; all remediated across Phases 0–3 ([`REMEDIATION_PLAN_2026-07-23.md`](repos/kindredaccess/docs/audits/REMEDIATION_PLAN_2026-07-23.md)): deletion made atomic (`transaction.atomic` + `select_for_update`, file removal via `on_commit`), reporter-filed unresolved reports anonymized instead of erased (a victim leaving no longer destroys the open case), held photos forced non-public with a staff-only media-proxy path, hold extended to flagged-but-unreported messages, deletion/retention copy made honest (no more unconditional-deletion promises), report rate limit stopped counting GET form loads + themed accessible 429, outbox hardened (partial unique constraint, cron locking, no-ADMINS dead-letter), password-meter colours moved into the design-token gate (test now scans `<script>` blocks too). 545 tests. [PR #19](https://github.com/BeauAccessSolutions/kindredaccess/pull/19) squash-merged → **deployed same day**: migrations 0036–0044 applied (prod had pre-existing drift at 0035), `retry_safety_notifications` cron installed in www-data's crontab (the durable-alert outbox is inert without it), live smoke tests green (no-JS signup fallback, hold disclosure, crisis links all serving). **Deploy archaeology — the 4th org-transfer deploy break:** prod had been silently undeployable since the org move — droplet origin still pointed at `Beaudoin0zach/...`, the deploy key was auto-disabled by the transfer, AND the org had `deploy_keys_enabled_for_repositories=false`; all three fixed (origin re-pointed, org toggle ON — must stay on, key re-added **read-only**, id 158152668). Same family as CIT's 3× `deploy_on_push` breaks and the marketing-site Netlify break: **after any owner change, spot-check the deploy *credential*, not just the webhook** — signature here was `ssh -T` greeting `Hi org/repo!` while fetch said "Repository not found" (= key `enabled:false` in `gh api repos/.../keys`). Deploy learnings amended into [`DEPLOY_RUNBOOK_2026-07-23.md`](repos/kindredaccess/docs/deployment/DEPLOY_RUNBOOK_2026-07-23.md) (`bb7e1af`): deploy as root+`www-data` (deploy/README's `deploy`-user assumption is wrong on the real box — not in the www-data group), and expect ~5–10s of warmup 500s post-restart before diagnosing. **Launch gate is now entirely non-code:** counsel decisions D1–D8 consolidated in [`LAUNCH_DECISIONS_COUNSEL_BRIEF_2026-07-23.md`](repos/kindredaccess/docs/audits/LAUNCH_DECISIONS_COUNSEL_BRIEF_2026-07-23.md) — most urgent now that it's live: the Terms still promise NCMEC reporting with no registered path (D1/D2), and the deployed evidence hold is a stopgap awaiting the counsel retention model incl. §2258A subscriber-identity preservation (D4/D6).) · 2026-07-23 (**Access Atlas — Capacitor 8.4.2 bump + native camera capture (repo-side) merged.** Two PRs from a Capacitor review: [#26](https://github.com/BeauAccessSolutions/access-atlas/pull/26) (`ff1e288`) bumps `@capacitor/{cli,core,ios}` 8.4.1 → 8.4.2 — a patch whose only change is Android-specific (image-capture URI perms), so zero functional effect on this iOS-only wrapper; no Capacitor 9 exists. [#27](https://github.com/BeauAccessSolutions/access-atlas/pull/27) (`d657249`) lands the **repo-side** of the App Review 4.2 value-add: native camera capture on the confirm flow (`public/confirm-camera.js`, native-only — no-ops on web; feeds `Camera.getPhoto()` into the existing `#photo` field, no backend change), a scoped `script-src 'self'` carve-out on the confirm route only (**ADR-0002**; browsing + `/contribute/submit` stay zero-JS), and an a11y assertion locking the one-self-hosted-script contract. Scope: `docs/ios-camera-evidence-scope.md`. Verified: `astro check` clean, a11y 77 passed, CSP scoped correctly per route, script no-ops on web. **Still needs a Mac:** `npm i @capacitor/camera` + `npx cap sync ios`, Info.plist usage strings, on-device VoiceOver round-trip (biggest unknown: plugin reachable via the global bridge without bundling). Branches deleted. Peer PR [#17](https://github.com/BeauAccessSolutions/access-atlas/pull/17) (providers-list count wording) remains open, untouched.) · 2026-07-23 (**Access Atlas — PRE_DEPLOY migration job BUILT ([PR #25](https://github.com/BeauAccessSolutions/access-atlas/pull/25), merged `c3bc183`), activation pending.** Closes the recurrence gap from the 500 incident below: a `migrate` PRE_DEPLOY job runs `supabase db push` against prod before the `web` version is promoted (the CIT `prisma migrate deploy` analog; a non-zero exit halts the deploy). Pieces: `scripts/migrate-deploy.sh` (fail-closed — exits non-zero if `SUPABASE_DB_URL` unset, so a missing secret halts the rollout rather than shipping code ahead of schema; `--dry-run` = drift check), `.do/migrate.Dockerfile` (tiny job image carrying `supabase/` + the script, separate from the web image), the `jobs:` entry in `.do/app.yaml`, and `migrate:deploy`/`migrate:check` npm scripts. Uses the same tool + `schema_migrations` table as local `supabase db reset` (no second runner to drift) and the history baselined below, so the first run is a no-op. Validated: shell syntax, the fail-closed guard, `doctl apps spec validate`, and a real `docker build` + run. Branch deleted. ⬜ **NOT yet active on prod** — takes effect ONLY when a `SUPABASE_DB_URL` secret (SESSION-mode pooler string, port 5432, percent-encoded — NOT the 6543 transaction pooler, NOT the IPv6-only direct host) is set AND `doctl apps update --spec .do/app.yaml` is applied. Apply caveats: repo `KEYCLOAK_ISSUER` matches live (safe), but the spec's `github.repo` still reads the old `Beaudoin0zach` org (redirects work; bump to `BeauAccessSolutions` while applying).) · 2026-07-23 (🔴 **Access Atlas — prod list pages were 500ing (schema drift); fixed + migration history baselined.** After #24 deployed, `/providers/` and `/places/` returned **HTTP 500** while `/` stayed 200 — surfaced when verifying the accent on the live site. **Root cause was NOT #24** (a CSS + one-class change can't 500 a data fetch): the prod Supabase DB (`gclxpbjifxwrhueopdta`, `ACTIVE_HEALTHY`, 163 real listings) was **missing the `listings.coords_source` column** that `getListings` selects — migration `0011` had never been applied to prod, so PostgREST rejected the query and the un-caught throw became a 500. The home page hid it because it doesn't query listings, and the DB being healthy made it *look* fine. **Deeper cause:** prod had **no migration-tracking table at all** (`supabase_migrations.schema_migrations` absent) — migrations were applied ad hoc, so code shipped ahead of the schema. Prod was also missing `0008` (moderation_audit) + `0010` (photo_reports); `0009`'s grant was already present. **Fix (verified against real Postgres via the Supabase MCP):** applied `0008` + `0010` + `0011` (all additive; the two new tables ship RLS deny-public-read — security advisor shows **no new advisories**), then **baselined all 11 migrations `0001`–`0011` into a freshly-created `schema_migrations`** so `supabase db push` treats them as applied going forward. **Verified live:** `/providers/` + `/places/` now **200**, 88 cards render, 15 violet representation tags present, `color-scheme` active — so #24's accent is confirmed on the deployed site (real listing "Apnea Care Inc" shows both Disabled-owned + Disabled-led in violet). 🟡 **Recurrence fix BUILT ([PR #25](https://github.com/BeauAccessSolutions/access-atlas/pull/25), merged), activation pending:** a `migrate` PRE_DEPLOY job (`supabase db push`) now lives in the repo spec — but it is INERT until `SUPABASE_DB_URL` is set + `doctl apps update --spec` is applied (see the newer log entry at the top). Until then the deploy still ships code without migrations. Pre-existing (unrelated) security lints to revisit: 2× `security_definer_view` (`attribute_claim_status`, `evidence_photos`) + a public-bucket-listing warning on the `evidence` bucket.) · 2026-07-23 (**Access Atlas — representation-accent + `color-scheme` design PR #24 merged to `main`, branch deleted.** A `/bas-design-review` of the colour tokens, run alongside the copy work below, surfaced two fixes, both in [#24](https://github.com/BeauAccessSolutions/access-atlas/pull/24) (`ec9d8ad`): (1) the two provider axes are orthogonal — *competence* (disability-literate) vs *representation* (disabled-owned/led) — but `ListingCard` rendered all three tags in the same teal, so the distinction the app is built on was invisible on the card; representation tags now carry a **violet accent** (`.tag--rep`) that is hue-separated from all five validation tones and the teal brand (so it never reads as a trust status) and always paired with the tag's text label (colour is reinforcement, never the sole signal — §5/WCAG 1.4.1); verified numerically in both themes (~7.2:1 ink-on-wash, 8.3–9.2:1 ink-on-canvas). (2) **C4 gap:** `global.css` had a full dark token set but never declared `color-scheme`, so native control chrome (scrollbars, `<select>`, date picker) could render light-on-dark — now `color-scheme: light dark` on `:root`, pinned to `dark`/`light` on the manual `.theme-*` overrides. Guarded by a new `tests/unit/contrast.test.ts` that reads `global.css`, extracts the accent tokens per theme, and recomputes WCAG contrast (fails if a hex drifts below AA or the two dark blocks fall out of sync). axe-core + zero-JS a11y suite green (76), unit 122, `astro check` clean; browser-verified light + dark. Merged by direct merge (same self-approval constraint as #22/#23); branch `design/representation-accent` deleted. This carries the two-axis distinction #23 restored in prose into the visual layer.) · 2026-07-23 (**Access Atlas — About-surface copy PRs #22 + #23 both merged to `main`, branches deleted.** [#22](https://github.com/BeauAccessSolutions/access-atlas/pull/22) (copy: claim the voice on the About surface) merged (`3728b90`). A review of #22 had flagged three honest-labeling copy defects (§2/§4), all fixed in follow-up [#23](https://github.com/BeauAccessSolutions/access-atlas/pull/23), merged (`23b8341`): the homepage overstated claim provenance ("every claim comes from a disabled person who went there" → the real self-reported→community-confirmed labels, since submitters can create zero-confirmation claims and the access-identity tag is optional); home + About summary copy narrowed providers to "disabled-led," dropping the disability-literate and disabled-owned axes (both restored, matching the homepage body); and the GOV.UK 41% figure was unqualified ("found 41%" → "flagged or prompted manual inspection for 41%," since detected errors/warnings were 37% and 41% includes manual-inspection prompts). Both merged by **direct merge** — the author can't self-approve and the repo has no branch protection requiring a review (a recurring §1b constraint on this single-maintainer portfolio). `npm run check` clean, browser-verified home/about/accessibility. Merged branches `copy/voice-remediation-2026-07` + `copy/honest-labeling-followup` deleted locally and on origin.) · 2026-07-23 (**`/platform-status` reconciliation pass — 3 stale PR counts fixed.** Ran with `gh` flipped to the `LangworthyWatch` account again (the recurring §1b failure mode), which silently zeroed KindredAccess's PR count; switching to `Beaudoin0zach` and re-querying found real drift beyond the account issue: **KindredAccess** actually has 4 open PRs, not 0 (#19 child-safety-policy matches the entry below, plus #16/#12/#7 untracked here before now); **a11y-probe** actually has 10 open dependabot PRs, not the "0, closed/merged" this doc previously claimed; **Access Atlas** has a new PR #22 not yet reflected. Benefits Navigator's own row checked out accurate against live state.) · 2026-07-23 (**Benefits Navigator — P1 audit fixes pushed, no PR yet.** Worked through all P1 items plus several P2 items from the app's own `TODO.md` (2026-06-09 audit): VSO `case_list`/`reports` N+1 queries fixed (`GapCheckerService.get_triage_label` made prefetch-friendly instead of issuing `.exclude()` per case; caseworker urgent/overdue counts now one annotated query instead of 2 queries per caseworker); `accept_invitation`'s 3 writes wrapped in `transaction.atomic`; missing "not legal advice" disclaimers added to the statement-generator pages; WCAG `aria-live` moved onto the actual persistent HTMX swap target across 9 templates (it was misplaced on a child element that gets destroyed and recreated on every swap in the appeals-checklist case, so screen readers never heard the update); a stored-XSS vector in `supportive_tags.py` (`mark_safe()` on an f-string containing unescaped admin-editable message content) fixed with `format_html()`; two silently-swallowed audit-log-write failures (login/logout) now `logger.exception()` instead of `pass`; `acks_late=True` added to the M21 scraper Celery tasks; a Redis failure during the health-check's queue-length lookup no longer vanishes silently. Along the way, found and fixed a related crash in 3 templates: `{{ x.get_full_name|default:x.email }}` 500s when `x` is a null FK, because Django resolves a filter's *argument* differently from its base variable and doesn't catch the failure — same `case.assigned_to` pattern in `case_list.html`, `case_detail.html`, and `document_share.html`. 1029 tests passing, ruff/black clean. Commit `b56accc` pushed to `fix/seed-documentation-content`, whose earlier PR (#38) had already merged separately before that session started; **[PR #39](https://github.com/BeauAccessSolutions/benefits_navigator/pull/39) opened 2026-07-23** for the unmerged commits (P1 fixes + peer-review remediation + a fresh Codex-audit log).) · 2026-07-23 (**CIT dev-environment fixes, two small merged PRs.** [#65](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/65): ESLint's build-output ignores only matched at the repo root, so nested `.claude/worktrees/*` (which live inside the checkout) leaked their independent `.next` build artifacts into lint runs — surfaced as real errors in minified third-party code that blocked a `git push` on its pre-push lint hook; fixed by prefixing the ignore globs with `**/`. [#66](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/66): local `npm run dev` was 500ing because Turbopack's workspace-root auto-detection picked up a stray lockfile in an ancestor directory outside the repo and resolved `node_modules` from the wrong place — fixed by pinning `turbopack.root` to `process.cwd()`; also scoped the CSP's `unsafe-eval` to non-production builds only, since React dev mode's error-overlay/HMR tooling needs it but the production CSP correctly omits it. Neither touches production behavior.) · 2026-07-23 (**KindredAccess PR #19 — peer-review safety remediation pushed, all blockers closed.** A 2026-07-21 peer review scored the branch across accessibility/safety/Django-quality/security/UX and blocked ship on one critical finding (account deletion erased safety evidence) plus four lower-severity gaps. All five now addressed on `fix/child-safety-policy` (commit `6fd068b`): account deletion now retains messages/moderation-photos/reports when an unresolved report exists against the deleting user (`Message`/`PhotoModerationQueue.retained_for_review` — a stopgap, not the counsel-gated retention policy); a `SafetyNotificationOutbox` + `retry_safety_notifications` command makes safety-alert emails retry instead of silently dropping; the rate limiter's token-bucket get/set race is now an atomic add/incr, `/report/` has its own stricter per-user bucket, and exact-duplicate report resubmissions are deduped without touching genuinely distinct incidents; signup now has a real non-JS fallback (signed timing token + honeypot, gated on a `js_enabled` flag so JS-capable clients still can't skip hCaptcha). A draft [`NCMEC_ESCALATION_RUNBOOK_DRAFT_2026-07-22.md`](repos/kindredaccess/docs/audits/NCMEC_ESCALATION_RUNBOOK_DRAFT_2026-07-22.md) is written for counsel/ops but not adopted. 535/535 tests passing, CI green (lint+test), pushed to origin. **Still open (counsel/ops-gated, not code):** the actual retention-vs-deletion policy, and NCMEC ESP registration status — `terms.html:159` still promises reporting the product can't yet fully back. PR #19 not yet merged to `main`; KindredAccess deploys manually via SSH, so none of this reaches the live site until both land.) · 2026-07-22 (**CIT `deploy_on_push` broke on a THIRD org move, fixed same day.** CIT's repo moved again, `Beaudoin0zach/Chronic-Illness-Tracker` → `BeauAccessSolutions/Chronic-Illness-Tracker` (same owner ID) — same failure mode as the marketing-site Netlify break above: the App Platform GitHub-App webhook doesn't follow a repo transfer, so 9 merged PRs that day produced zero deployments. Fix: `doctl apps spec get` → edit both `github.repo` fields → `doctl apps update --spec`; **the webhook re-linked automatically on the spec update alone**, no dashboard reauth needed — confirmed via a real push building through to `ACTIVE`. Reconciling the fix surfaced a second, older, undocumented drift: the committed `.do/app.yaml`'s `KEYCLOAK_ISSUER` still read the retired `id.kindredaccess.org` while the live app had been on the canonical `id.beauaccesssolutions.com` since the 07-16/17 migration below — both hostnames resolve to the same Keycloak instance so nothing was broken, just unrecorded. Both fields fixed in [CIT PR #67](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/67), merged, verified deployed. Lesson: any repo that's moved orgs before should have its App Platform (or Netlify/Vercel) `repo` field spot-checked after a future move — this is now CIT's third transfer to break the same way.) · 2026-07-22 (**CIT peer-review remediation plan — PR 7 + PR 8 batches, all merged to `main`.** Nine PRs (#54, #55, #56, #57, #59, #60, #61, #63, plus drive-by #58): entries-route query validation (400 not 500), symptom/exposure/sleep rating-slider not-rated affordance, a new DB-backed integration suite against real Postgres (had to run under `tsx`/`node:test` instead of Jest — Prisma 7's WASM query compiler can't load under Jest's CJS transform), export rate-limiting + cleanup cron, signed export-download tokens (`docs/adr/008`), `TagOnEntry` uniqueness/FK indexes, Bearer auth now passes the edge proxy for native clients with a documented CSRF re-examination, session-IP capture via `clientIp()`, email-verification token invalidation on resend, and dead-dependency cleanup (`uuid`, `recharts`, unused `visualizations` module — `useReducedMotion` deliberately kept, still documented as required future infra). Tracked issue [CIT #62](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/issues/62) files the TEMPORARY OIDC diagnostic for later removal. CIT is now 20 commits ahead of where this tracker last reflected it.) · 2026-07-20 (**CIT a11y wayfinding pass** — a `/bas-design-review` of CIT navigation found nav controls below the WCAG 2.2 SC 2.5.8 target-size floor (log entry-type tabs ~20px, the one blocking item; plus mobile menu toggle, Settings→regimen links). Fixes open in [CIT PR #49](https://github.com/Beaudoin0zach/Chronic-Illness-Tracker/pull/49). Deferred wayfinding findings (mobile thumb-zone bottom nav, onboarding traps browser Back, per-route tab titles, radius drift, auth home anchor) captured in CIT `docs/plans/logging-ergonomics-implementation.md` §7 — that plan is an untracked draft, not yet committed.) · 2026-07-20 (portfolio **writing style-eval** run across all 8 apps — report at `docs/style-eval-2026-07-19.md`; quality (3.4–4.6) beats voice (1.9–3.9) in every scored app, 12 correctness defects logged incl. an incoherent KindredAccess minors-clause and a page-repair Web-Store ship-blocker, remediation chips spun per app. Prior state unchanged below.) · 2026-07-19 (🔴 **marketing-site apex is serving a STALE Netlify build** — the org transfer broke Netlify's GitHub App so it stopped building 4 commits ago; a peer's Cloudflare Pages deploy is fully current at `beau-access-solutions.pages.dev` but the apex DNS was never cut over. Founder note shipped, live on Pages, not on the apex)
**Legend:** ✅ done · 🟡 in progress · ⬜ not started · ⏳ blocked / waiting on input

---

## 1. Portfolio & platform onboarding

| App | Platform role | Stack | Remote | CLAUDE.md pointer | Onboarding |
|---|---|---|---|---|---|
| **Chronic Illness Tracker** | App #1 (PHI) | Next.js + Postgres | `BeauAccessSolutions/Chronic-Illness-Tracker` (origin, moved 3× — was `Beaudoin0zach`, before that `kbeaudoin001`) | ✅ pointer **merged to `main`** (PR #1) | 🟡 leading · a11y target-size fix merged ([PR #49](https://github.com/Beaudoin0zach/Chronic-Illness-Tracker/pull/49), 2026-07-20) · **peer-review remediation plan (PR 7 + PR 8 batches) fully merged to `main`, 9 PRs, 2026-07-22** — real-Postgres integration suite, export rate-limit + signed download tokens, `TagOnEntry` FK indexes, Bearer-auth-through-proxy for native clients, session-IP capture, verification-token invalidation, dead-dep cleanup; tracked follow-up: [CIT #62](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/issues/62) (remove TEMPORARY OIDC diagnostic once platform-login 401s are root-caused) · **3rd org-move `deploy_on_push` break fixed same day + `.do/app.yaml` reconciled ([PR #67](https://github.com/BeauAccessSolutions/Chronic-Illness-Tracker/pull/67)), 2026-07-22** |
| **KindredAccess** | App #2 | Django + Channels | `BeauAccessSolutions/kindredaccess` (moved — was `Beaudoin0zach`; droplet remote + deploy key repaired 2026-07-23) | ✅ pointer **merged to `main`** (PR #2) | 🟡 OIDC RP integrated ahead of seq. (PR #4) · **child-safety remediation [#19](https://github.com/BeauAccessSolutions/kindredaccess/pull/19) merged + DEPLOYED to prod `b5b6036` 2026-07-23** (see Last-updated note; launch gate now counsel decisions D1–D8) · as of 2026-07-23: **3 PRs open** — #16 design-residuals, #12 core/views split refactor, #7 photo-moderation off request thread |
| **Benefits Navigator** | Candidate (sensitive) → Member pending [ADR-005](docs/adr/005-benefits-navigator-data-posture.md) | Django + AI | `Beaudoin0zach/benefits_navigator` | ✅ pointer **merged to `main`** (PR #23, admin override past review gate) · as of 2026-07-23: 13 PRs open — #37 claims-affordance + #36 appeals-protected-media (unreviewed fix PRs) + #28/#27 design-review a11y + #24 membership + #22 governance, rest dependabot · P1 audit fixes (N+1 queries, `transaction.atomic`, WCAG aria-live, XSS fix, audit-log-swallow fixes, M21 `acks_late`) + peer-review remediation + a fresh external-audit log now in **[PR #39](https://github.com/BeauAccessSolutions/benefits_navigator/pull/39)** (opened 2026-07-23) · **first app run against the platform multi-role test matrix, 2026-07-23** — 3 blockers filed ([#59](https://github.com/BeauAccessSolutions/benefits_navigator/issues/59) caseload scoping off by default, [#60](https://github.com/BeauAccessSolutions/benefits_navigator/issues/60) no idempotency, [#61](https://github.com/BeauAccessSolutions/benefits_navigator/issues/61) no acting role in audit entries), 2 fixed in [PR #62](https://github.com/BeauAccessSolutions/benefits_navigator/pull/62), and `main`'s red CI fixed in [PR #64](https://github.com/BeauAccessSolutions/benefits_navigator/pull/64); **both green, both stacked, both blocked on a review the solo owner can't self-supply** ⏳ · ⚠️ **the PR inventory in this row is stale** — live open PRs as of 2026-07-23 are #64, #63, #62, #57, #56, #55, #54, #52 (none of #37/#36/#28/#27/#24/#22/#39 are still open); needs a `/platform-status` pass, not a hand-patch | ⬜ |
| **Access Atlas** (access-directory) | Member (identity) | Astro | `Beaudoin0zach/access-atlas` | ✅ pointer on `main` · ✅ **invariants PR #1 merged to `main`** | 🟡 onboarded · invariants #2/#3/#4 ✅ · identity #1 🟡 (Keycloak BFF auth landed) · #5 ⏳ · evidence-photos PR #18 merged, PR #17 open · as of 2026-07-23: **About-surface copy [PR #22](https://github.com/BeauAccessSolutions/access-atlas/pull/22) + honest-labeling follow-up [PR #23](https://github.com/BeauAccessSolutions/access-atlas/pull/23) + design [PR #24](https://github.com/BeauAccessSolutions/access-atlas/pull/24) all merged to `main`** (see Last-updated note; #22/#23 = three §2/§4 copy fixes — claim-provenance, both provider axes restored, GOV.UK 41% qualified; #24 = violet representation accent `.tag--rep` + `color-scheme` C4 fix, contrast-guarded), branches deleted |
| **Disability Wiki** | Member (identity) — Access-Atlas-shaped sibling | Astro Starlight (post-Wiki.js) + Cloudflare Pages | `Beaudoin0zach/disability-wiki` (public) | ✅ `docs/platform-membership.md` (governance pointer + invariants fallback; URL repointed to `bas-platform` 2026-07-16) | 🟡 onboarded (self-declared 2026-07-14, now reflected here) · Keycloak BFF auth + Supabase moderation store code-complete on branch (Phases 1–3), fail-closed · ⏳ live wiring needs a Supabase project + registered Keycloak client + data-controller sign-off. **Read path is DB-free static** — Supabase is write-only staging, never a read dependency (crisis pages must load offline) |
| **a11y-probe** | Standalone / CI a11y | Reddit Devvit | `Beaudoin0zach/a11y-probe` (private) | ✅ repo initialized · `main` pushed (initial commit 2026-07-14, incl. 44px touch-target fix); design-review AA fixes on `main` (2026-07-16); real README + descriptive post title on `docs/real-readme` (2026-07-19, pushed, **no PR yet**) · **10 open PRs as of 2026-07-23** — all dependabot (`#1`–`#10`, opened 2026-07-14); prior "0 open, closed/merged" claim was stale/wrong | n/a |
| **page-repair** | Standalone; patterns → `ui` | Browser extension | `LangworthyWatch/page-repair` (canonical) · `Beaudoin0zach/page-repair` (origin) | ✅ branch pushed · ⏳ pointer PR status unverifiable from this account | n/a |
| **Marketing site** | Company site (not a platform app) | Astro + Netlify | `Beaudoin0zach/bas-website` (private) | — | 🟢 **LIVE** <https://beauaccesssolutions.com> · remade around the app portfolio (consulting kept) · **links the live apps + contrast/token CI gate** (2026-07-18) |

**Pointer-PR rollout — DONE for all four verifiable repos; pointers are merged to `main`.**
- CIT — ✅ merged (PR #1)
- KindredAccess — ✅ merged (PR #2)
- Benefits Navigator — ✅ merged (PR #23, admin override past the Code Owner review gate)
- Access Atlas — ✅ pointer already on `main`; invariants PR #1 also merged
- page-repair — pointer PR status **unverifiable** from this GitHub account (`LangworthyWatch/page-repair` is third-party); confirm directly there.

---

## 1b. Repo & disk map

The full inventory of every BAS repo → GitHub remote + local checkout path(s), plus the
duplicate-clone and folder-name-vs-repo-name hazards that caused the earlier wrong-repo
Netlify incident, lives in the **private** `bas-internal` repo
(`infra/repo-and-disk-map.md`) — it enumerates private repo names, disk paths, and
unrelated personal projects, so it stays out of this public hub.

~~One hazard worth stating here: **Disability Wiki has no clone under `repos/`**~~ —
**resolved.** `repos/disability-wiki` has been a symlink to `~/projects/disability-wiki`
since 2026-07-16, and `platform-status.sh` now probes it like every other app (verified
2026-07-19: reports branch, ahead/behind, worktree state, and all 4 open PRs).

⚠️ **ALL 11 REPOS MOVED TO A GITHUB ORG (2026-07-19).** Everything is now under
**`BeauAccessSolutions/`**, not `Beaudoin0zach/` — verified repo-by-repo with `gh repo view`:
CIT, page-repair, disability-wiki, bas-website, bas-apps, access-atlas, a11y-probe,
benefits_navigator, kindredaccess, kindredaccess-ios, bas-platform. History is intact and GitHub
redirects the old paths, so git keeps working. Consequences:
- **These docs are stale in 38 places** (TRACKER 34, README 3, PLATFORM 1). Not yet rewritten — it's
  a mechanical pass but was deferred while a peer session was committing here.
- **9 of 10 local checkouts already point at the new org**; `page-repair` and `bas-apps` still have
  the old remote (cosmetic — redirects work).
- 🔴 **CONFIRMED — the marketing site is serving a STALE Netlify build, and this exact failure is why.**
  Netlify's GitHub App authorization is *per-account* and did **not** follow the repo into the org, so
  Netlify silently stopped building at commit `5d88556` (the last push made while the repo was still
  `Beaudoin0zach/bas-website`). Four commits since — ADA-post removal, photo resize, the **Cloudflare
  Pages migration**, and the **founder note** — pushed to GitHub fine and were never built. Git pushes
  succeeding via redirect is exactly what hid it. **Compounding twist:** a peer session meanwhile ported
  the site to **Cloudflare Pages** (`ce25589`), and `beau-access-solutions.pages.dev` is **fully current**
  (has everything, auto-builds from the org repo) — but **DNS was never cut over**: the apex still
  resolves to `75.2.60.5` / `server: Netlify`. So the fresh build exists on a URL nobody visits while the
  public apex shows a 4-commit-old copy. ⬜ **Fix: cut apex DNS `beauaccesssolutions.com` → the Pages
  project** (owner decision — outward-facing infra on a live domain, not automatable here). That
  unfreezes the migration + founder note + all stranded commits at once. Same failure shape as the
  2026-07-16 wrong-repo incident below — second silent-deploy break in three days.
- **5 repos are now public:** page-repair, disability-wiki, access-atlas, bas-platform, and
  **benefits_navigator**.

🟡 **BN `.env.docker` is in public history — assessed, mostly benign, one item open (2026-07-19).**
`.env.docker` was committed in BN's initial commit (`9bff52e`) and removed in `c582113` ("Fix all P0
critical issues — secrets…"), but history retains it, and BN is now public. **Verified reachable
anonymously** (logged-out browser context, raw blob → HTTP 200, 22 keys). ✅ **Contents are a
local docker-compose dev file, not production credentials:** `DATABASE_URL` is
`postgresql://db:5432` (a compose *service name*), Redis/Celery are `redis://redis:6379` with no
password, Stripe keys are `sk_test_`/`whsec_yo…` placeholders ~21 chars (a real Stripe key is ~107),
`ALLOWED_HOSTS=localhost`, `DEBUG=True`. ⬜ **One thing still worth checking:** `SECRET_KEY` (24
chars). It differs from the current local `.env`, and everything around it is dev-local, so it is
very likely the dev key — but prod's is a DO `SECRET` that can't be read, and §6 still lists prod
`SECRET_KEY` rotation as an open decision. Compare the first/last few chars in the DO console against
`git show 9bff52e:.env.docker`; **same → rotate**, different → pure housekeeping. History rewriting
(`git filter-repo`) is only worth the disruption if that check comes back bad — it cannot retract
what is already cloned or indexed. ✅ **The other four public repos never committed a secret file.**
⚠️ *Method note: the first pass called these "looks real" off a value-length heuristic and overstated
the risk; what settled it was reading where the values pointed, not how long they were.*

A second, subtler hazard: **`platform-status.sh` reports `open PRs: 0` when `gh` is signed
in as the wrong account** — it does not distinguish "no PRs" from "cannot see this repo."
Observed 2026-07-18: `gh` had flipped to `LangworthyWatch`, and the script reported CIT as
having 0 open PRs while [#41](https://github.com/Beaudoin0zach/Chronic-Illness-Tracker/pull/41)
was open (a direct `gh pr list` errored with "Could not resolve to a Repository"). The
account flips back on its own, so this is not a one-off. Run `gh auth switch --user
Beaudoin0zach` before trusting a PR count, and treat a `0` as unverified until then —
**the script currently fails open on this, which is the exact failure mode the board exists
to prevent.**

Recurred 2026-07-19 with a **second symptom**: the flip also breaks *writes*, not just
reads. `gh pr create` failed twice with `GraphQL: must be a collaborator
(createPullRequest)` while `gh api repos/…` showed `{"admin":false,"push":false,"pull":true}`
— the account had flipped to `LangworthyWatch` mid-session. `git push` over SSH kept working
throughout, so the branch published fine and only PR creation failed, which makes the cause
easy to misread as a GitHub outage. It resolved on retry a few minutes later without any
intervention. If a `gh` write fails with a permissions error, check `gh auth status` before
believing the error.

---

## 2. Deployment & hosting

Where each app is meant to run, and whether it's actually there yet. "Artifact" =
what gets shipped; "Trigger" = how a deploy happens.

| App | Artifact | Host / platform | Config source | Trigger | URL / DNS | Status |
|---|---|---|---|---|---|---|
| **Chronic Illness Tracker** | Next.js web + managed Postgres 17 | **DigitalOcean App Platform** (region `nyc`, `basic-xxs`) | [`.do/app.yaml`](repos/chronic-illness-tracker/.do/app.yaml) — repo `BeauAccessSolutions/Chronic-Illness-Tracker`, health `/api/health`, pre-deploy `prisma migrate deploy` | `deploy_on_push` on `main` | 🟢 <https://chronic-illness-tracker-7o7fw.ondigitalocean.app> | 🟢 **live** (auto-deploys `main` — webhook re-linked 2026-07-22 after a repo transfer broke it; see §6) · also the API backend the **Baseline** iOS app calls — see §2b |
| **Benefits Navigator** | Django + Celery + Redis | **DigitalOcean App Platform** (region NYC, App ID `2119eba2-07b6-405f-a962-d40dd6956137`) | [`DEPLOYMENT.md`](repos/benefits-navigator/DEPLOYMENT.md), `Dockerfile.prod` · **`PRE_DEPLOY` job seeds reference content** (see §2c) | git push | 🟢 <https://vabenefitsnavigator.org> (PRIMARY; `www` alias **301s to apex** — see [canonical-host-redirects](docs/deploy/canonical-host-redirects.md)) · <https://benefits-navigator-staging-3o4rq.ondigitalocean.app> (DO default) | 🟡 **staging live** · ⬜ prod · ⚠️ despite the app name, this app **serves the real domain** — verify OIDC on `vabenefitsnavigator.org`, never the `ondigitalocean.app` URL (the Keycloak client registers only the prod callback, so login *cannot* complete there) |
| **KindredAccess** | Django web backend + Capacitor mobile shell | **DigitalOcean Droplet** (Ubuntu 22.04, $12–18/mo) | [`DIGITAL_OCEAN_DEPLOYMENT.md`](repos/kindredaccess/DIGITAL_OCEAN_DEPLOYMENT.md) + `deploy/` systemd units (Gunicorn HTTP + **Daphne WebSockets**, nginx `/ws/` routing) — KA PR #3 | **manual (SSH)** — no auto-deploy | 🟢 <https://kindredaccess.org> | 🟢 **live** · the site the KindredAccess iOS wrapper loads (§2b). Manual deploy: `main` edits only reach the site + app after an SSH redeploy. `www` **301s to apex** (nginx, 2026-07-18 — see [canonical-host-redirects](docs/deploy/canonical-host-redirects.md)) |
| **Access Atlas** (access-directory) | Astro SSR (zero-JS surface) + Supabase | **DigitalOcean App Platform** (Dockerfile from GitHub) | [`.do/app.yaml`](repos/access-directory/.do/app.yaml) — repo `Beaudoin0zach/access-atlas`, `deploy_on_push: true` on `main` | `deploy_on_push` on `main` | 🟢 <https://access-atlas-qd464.ondigitalocean.app> | 🟢 **live** (deployed 2026-07-10, auto-deploys `main`) · the site the Access Atlas iOS wrapper loads (§2b) · 🟡 **migration step BUILT, not yet applied** — a `migrate` PRE_DEPLOY job (`supabase db push`) is in the repo `app.yaml` ([PR #25](https://github.com/BeauAccessSolutions/access-atlas/pull/25), merged) but INERT until `SUPABASE_DB_URL` is set + `doctl apps update --spec` applied; until then deploys still ship code without migrations (the 2026-07-23 500 cause). Migration history was baselined 2026-07-23 so the job's first run is a no-op. **Apply-prep DONE ([PR #28](https://github.com/BeauAccessSolutions/access-atlas/pull/28), merged):** `app.yaml` `github.repo` bumped to the `BeauAccessSolutions` org on both components so applying the spec no longer reverts the (already-correct) live repo setting / risks the webhook. **Remaining owner steps:** set the `SUPABASE_DB_URL` secret (SESSION pooler, port 5432) in the DO console, then `doctl apps update a39a2fb9-… --spec .do/app.yaml` |
| **Disability Wiki** | Static Astro Starlight bundle (~540 pages); contribution write path = **Cloudflare Pages Function** + Supabase moderation queue | **Cloudflare Pages** | `site/` in `BeauAccessSolutions/disability-wiki` (`astro.config.mjs`, `wrangler.jsonc`) | `deploy_on_push` on `main` → Pages build (**hook silently died ~2026-07-19 after the repo transferred `Beaudoin0zach`→`BeauAccessSolutions`; reconnected 2026-07-23**) | 🟢 <https://disabilitywiki.org> | 🟢 **live** (verified 2026-07-23). ⚠️ **Platform lesson:** a GitHub→Pages hook can die on an org transfer with zero errors — merges sat unpublished for 4 days on a life-safety site. Now guarded by a `verify-live-deploy` CI job that polls the live signed OTA manifest for the merged SHA and red-Xes a merge that didn't publish. Static read path has **no DB dependency**; contribution backend still **inert** until Supabase + Keycloak client + sign-off land |
| **a11y-probe** | Reddit Devvit app (client + server bundle) | **Reddit Devvit** platform | [`devvit.json`](repos/a11y-probe/devvit.json) | `devvit upload` / `publish` | Reddit-hosted | 🟡 **repo initialized** (`Beaudoin0zach/a11y-probe`, private) · ⬜ not published to Devvit |
| **page-repair** (extension) | Browser extension (MV3) | **Chrome Web Store / AMO** | [`manifest.json`](repos/page-repair/manifest.json) v1.0.0 + icons · [`PRIVACY.md`](repos/page-repair/PRIVACY.md) · [`STORE_LISTING.md`](repos/page-repair/STORE_LISTING.md) · `dist/page-repair.zip` | store submission | store listing | 🟡 **submission-ready** · ⬜ not submitted (needs dev account + screenshots) |
| **page-repair** (credit proxy) | Cloudflare Worker + KV + **Durable Object** (credits) | **Cloudflare Workers** | [`proxy/wrangler.jsonc`](repos/page-repair/proxy/wrangler.jsonc) | `wrangler deploy` (manual) | 🟢 <https://page-repair-proxy.airboat-webcast-5u.workers.dev> | 🟡 **live but inert** — `ANTHROPIC_API_KEY` secret unset. Atomic-credit `CreditsAccount` DO merged to `main` (2026-07-14). ✅ **Concurrency gate cleared** — `wrangler dev` test (50 parallel requests on 1 credit → exactly **1** got past the spend, 49× `402`; balance correct) proves the DO closes the overspend race; `wrangler types` regenerated. Remaining to activate paid labeling: `wrangler secret put ANTHROPIC_API_KEY` + `wrangler deploy` |
| **Marketing site** | Astro static | **Netlify** | [`netlify.toml`](repos/marketing-site/netlify.toml) — build `npm run build` → `dist`, security headers (SPA catch-all removed; real 404 page added) | **continuous** (push to `bas-website` → auto-build) | 🟢 <https://beauaccesssolutions.com> | 🟢 **LIVE** (deployed 2026-07-16, remade around the app portfolio) · repo `Beaudoin0zach/bas-website` (private). Netlify site `620702da` under **beaudoin0zach@gmail.com** ("Baby Booty" team), ✅ **continuous deploy wired to `bas-website`/`main` and verified** (a push auto-built + published). ⚠ NOT `Beau-Access-Solutions` — see §6 |
| **Keycloak** (identity infra) | Self-hosted Keycloak + own DB | **DigitalOcean** (Droplet) | [docs/deploy/keycloak-digitalocean.md](docs/deploy/keycloak-digitalocean.md) | manual (SSH; Docker Compose in `/opt/keycloak`, droplet `bas-keycloak` 138.197.125.90) | 🟢 <https://id.beauaccesssolutions.com/realms/bas> (canonical) · 🟡 <https://id.kindredaccess.org/realms/bas> (dual-served, to retire) | 🟢 **prod live** — `bas` realm reachable on both hosts (verified 2026-07-18). **Canonical issuer migrated to `id.beauaccesssolutions.com` (2026-07-16/17)**; the old host stays dual-served until the three native builds carrying it are rebuilt (CIT Baseline `eas.json`, Access Atlas + KindredAccess Capacitor), then retires via a 301. ⚠️ **Droplet, not App Platform** — no `doctl apps` surface and no bindable DB refs; its `/opt/keycloak/.env` (dedicated `keycloak` DB user, private-VPC host) must be hand-updated on credential rotation |

**What this shows:**

- **DigitalOcean is the platform default** — CIT, Benefits Navigator, KindredAccess, Access Atlas, and Keycloak all run on DO (App Platform for CIT/BN/Access Atlas, Droplets for KA + Keycloak).
- **What's actually live today (verified 2026-07-16 via HTTP probes; identity re-verified 2026-07-18):** CIT web (`chronic-illness-tracker-7o7fw`), Access Atlas (`access-atlas-qd464`), **Disability Wiki (`disabilitywiki.org`)**, KindredAccess (`kindredaccess.org`), Benefits Navigator (`vabenefitsnavigator.org` — served by the DO app confusingly *named* `benefits-navigator-staging`; there is no separate prod app), marketing site (`beauaccesssolutions.com`), Keycloak **prod** (`id.beauaccesssolutions.com` canonical; `id.kindredaccess.org` still dual-served), and the page-repair credit proxy (inert until its API key is set). This is a big correction from prior versions of this doc, which listed most of these as "not deployed."
- **Auto-deploy vs manual:** CIT, Access Atlas, and Benefits Navigator deploy on push to `main`; **KindredAccess and Keycloak are manual** (SSH to the Droplet) — KA edits on `main` don't reach the site (or the iOS wrapper) until someone redeploys.
- **Production DNS is partially wired:** `kindredaccess.org`, `vabenefitsnavigator.org`, `disabilitywiki.org`, and `id.beauaccesssolutions.com` are live (the last is a grey-cloud/DNS-only Cloudflare A record — Caddy provisions its own cert, so proxying would break ACME); legacy `id.kindredaccess.org` still resolves during the native-build transition. CIT/Access Atlas still use DO-generated hostnames (no custom domain yet). **Both custom-domain apps now serve one canonical host** — `www` 301s to the apex on BN (App Platform ingress rule) and KA (nginx server block) as of 2026-07-18; before that both served `www` directly, splitting host-scoped sessions.
- **Still open:** the marketing site (needs a repo, then a Netlify connect); a11y-probe (Devvit publish); page-repair (store submission + proxy key).
- **Non-server distribution:** a11y-probe → Reddit Devvit, page-repair → browser-extension stores, and the three **iOS apps → TestFlight** (§2b) — none are hosts we operate.

---

## 2b. iOS / TestFlight

**All FIVE apps are on TestFlight as of 2026-07-18** — the 2026-07-18 upload round shipped issuer-migration rebuilds of the original three (all now authenticate natively against `id.beauaccesssolutions.com`, unblocking the old-host 301 retirement in §2) plus **first builds of Benefits Navigator and Disability Wiki**. Full architecture + the proven device-free archive→upload runbook: [docs/mobile-and-testflight.md](docs/mobile-and-testflight.md).

**Same-day follow-ups (2026-07-18 PM):**
- **DW 1.0 (2)** — fixed "every link goes home": Capacitor's default router is SPA-only; `WikiRouter` (DW PR #46, merged) resolves directory indexes and 404s honestly. Shared lesson captured.
- **TestFlight distribution now API-automated** — `scripts/asc-api.py` (ASC key `3SAY53224G`, App Manager). Root cause of "builds ready but never on the phone": **new app records had no beta groups**; created Internal groups (all-builds) + enrolled the aol tester for BN/DW via API, and swept export-compliance flags on every build (`usesNonExemptEncryption=false` via PATCH — no more per-build clicking). AA build 3 had been silently withheld on that flag.
- **BN 1.0 (2)** wraps `vabenefitsnavigator.org` (PR #33 merged); build (1) wrapped the DO URL where login can't complete — expired/do-not-distribute.
- **Icon family v4 approved** — masters in [design/app-icons/](design/app-icons/); not yet wired into asset catalogs (needs a build round).

| App (TestFlight name) | Build type | Source repo (⚠ remote?) | Loads / contains | How an edit reaches testers |
|---|---|---|---|---|
| **Access Atlas** — 1.0.0 (3) | Capacitor / WKWebView **wrapper** | `access-directory` (`capacitor.config.ts`) | the **live DO site** at runtime | redeploy the web app (auto on `main`) → relaunch app. **No new build** unless the native shell changes |
| **KindredAccess** — 1.0 (2) | Capacitor / WKWebView **wrapper** | **`kindredaccess-ios`** — ✅ pushed `Beaudoin0zach/kindredaccess-ios` (private) | **`kindredaccess.org`** at runtime | **SSH-redeploy** the site (manual) → relaunch app. No new build for content |
| **Baseline** = CIT — 1.0.0 (7) | **native Expo / React Native** (EAS) | **`bas-apps/apps/cit`** — ✅ pushed `Beaudoin0zach/bas-apps` (private; monorepo also holds shared `ui`/`auth`/`tokens`/`i18n` packages) | native RN screens; calls the CIT API backend | **`eas update`** (OTA, JS-only, no rebuild) or **`eas build` + `eas submit`** (native changes). EAS Update channels configured |
| **Benefits Navigator** — 1.0 (2) | Capacitor 8 / WKWebView **wrapper** | `benefits-navigator` `mobile/` (BN PR #30) | **`vabenefitsnavigator.org`** at runtime | redeploy the web app → relaunch app. ⚠️ build 1.0 (1) wrapped the `ondigitalocean.app` URL where the Keycloak callback isn't registered (login dead-ends) — superseded same-day by 1.0 (2); don't distribute (1) |
| **Disability Wiki** — 1.0 (**6**, on TestFlight) | Capacitor **8** **bundled-static** (offline-first — NOT a URL wrapper) | `disability-wiki` `app/` | bundles `site/dist` into the binary **plus a signed OTA content channel** | **Submission-ready as of 2026-07-23** (builds 4–6 uploaded). Full remediation shipped (`docs/app-remediation-plan.md`): the P0 was the bundle silently shipping a hotline-free abuse page — now a `verify-bundle` tripwire + `build-release.sh` make a stale bundle impossible; Cap 6→8; **ed25519-signed OTA channel** so crisis fixes reach installed apps without App Review; native crisis affordances (button, quick actions, status sheet, saved pages, share, Spotlight) for the §4.2 case; Dynamic Type. `/contribute` gap **resolved** (build-time hand-off to the live site). One-command releases via `app/fastlane` (`bundle exec fastlane beta`). Remaining: real-device pass + App Store submission (kit in `app/appstore/`) |

**Key facts:**
- **Two of three are thin webview wrappers** — their "app" is really the hosted website. Edits ship by **web deploy**, not a TestFlight rebuild. Only native-shell changes (icon, splash, `server.url`, plugins, version bump) need a new build.
- **Baseline (CIT) is the exception** — a genuine native app. Its own code edits need `eas update` (OTA) or a full `eas build` + submit. A server-side backend fix (like the AI date-range fix, 2026-07-14) reaches it only through the API, and only for features the native app actually has (it has **no AI-insights screen yet**).
- **Mobile source backup — complete:** `bas-apps` (Baseline + shared packages), `kindredaccess-ios`, `bas-frontend`, and `access-atlas-mobile` are all now pushed to private `Beaudoin0zach/*` repos. See §6.
- **External-review blocker (Access Atlas):** a bare webview wrapper is rejected under App Store Guideline 4.2; internal TestFlight (≤100) is fine. Clearing external review needs the camera evidence-photo feature. 🟡 **Repo-side landed 2026-07-23** ([PR #27](https://github.com/BeauAccessSolutions/access-atlas/pull/27), ADR-0002): `public/confirm-camera.js` — native camera capture on the confirm flow (no-ops on web) into the existing evidence-photo pipeline, plus a scoped CSP carve-out. **Remaining (Mac-side):** `npm i @capacitor/camera` + `npx cap sync ios`, Info.plist `NSCameraUsageDescription`/`NSPhotoLibraryUsageDescription`, and on-device verification (de-risk: plugin reachable via the global bridge without bundling). Scope: `access-directory` `docs/ios-camera-evidence-scope.md`.

---

## 2c. Seeded reference content (BN)

**Every content-backed page in Benefits Navigator was empty in production until 2026-07-18** — ~197
authored fixture records had never been loaded. Two independent causes: nothing invoked a loader at
deploy time (the `PRE_DEPLOY` job ran only `migrate`), and `glossary_terms_expanded.json` was
**orphaned** — no code path referenced it, so even a successful import would have loaded 50 of 90
glossary terms.

| Surface | Before | Now |
|---|---|---|
| `/exam-prep/glossary/` | 0 | 🟢 **89 terms** (90 records, one cross-file duplicate merged on the `term` upsert key) |
| `/exam-prep/` | 0 | 🟢 **7 guides** |
| `/docs/forms/` · `/docs/legal/` · `/docs/exam-guides/` | 0 | ⬜ **61 records prepared, not yet applied** |
| supportive messages | 0 | ⬜ 36 records, same pending spec change |

**How it ships:** the DO `PRE_DEPLOY` job. Fixtures are *not* all Django fixtures — `examprep`'s are
plain field dicts that `loaddata` would reject, so they go through `manage.py import_content
--fixtures` (idempotent `update_or_create`, no `--clear`). The `documentation`/`core` ones are real
Django fixtures and use `loaddata`.

⚠️ **The job's failure semantics were a landmine.** The command ended in `; exit 0`, so a failed
migration reported success and the deploy proceeded against an un-migrated database. The pending
spec change fixes this: `migrate … || exit 1` (blocks the deploy) while content seeding warns
without blocking. Shell logic verified across all four failure paths before shipping.

⚠️ **`loaddata` overwrites by pk on every deploy** — correct for reference content, but admin edits
to those records would be reverted. If that's unwanted, they belong in a one-time data migration.

⚠️ **`document_categories.json` is the canonical category taxonomy.** `va_forms.json` used to
redefine pks 1–3 with different names, so load order silently picked a winner; the duplicates were
removed (BN PR #38). Established by evidence: `va_forms_additional.json` references pks 4/5/6 which
exist only in the dedicated file.

---

## 3. Roadmap (from [PLATFORM.md](PLATFORM.md))

- **Phase 0 — Foundation** 🟡
  - ✅ Execution scoped ([docs/phase-0-execution.md](docs/phase-0-execution.md)) — owner split + decisions
  - ✅ DO deploy runbooks written ([Keycloak](docs/deploy/keycloak-digitalocean.md) · [CIT backend](docs/deploy/cit-backend-digitalocean.md))
  - 🟡 Keycloak: local dev scaffolded ([identity/dev/](identity/dev/)) · ⬜ prod stand-up on DO (needs Droplet + `id.` DNS)
  - ⬜ Deploy CIT backend to DO App Platform (spec: CIT `.do/app.yaml`)
  - ⬜ Monorepo scaffold (pnpm + Turborepo) + Expo skeleton
  - ⬜ Port CIT themes → reusable a11y-first `ui` primitives
  - ⬜ CI a11y + import-boundary gates
- **Phase 1 — Identity contract** ⬜
  - ⬜ OIDC clients + scopes on Keycloak
  - ⬜ `packages/auth` PKCE login + secure token storage
  - ⬜ Step-up (ACR) policy defined
- **Phase 2 — CIT as resource server** 🟡
  - ✅ Token-exchange spec (CIT `docs/mobile/auth-token-exchange.md`) — reconciled with the login-path seam
  - 🟡 `POST /api/auth/session` implemented — branch `feat/oidc-session-endpoint` (draft PR), 177 tests green. Verifies Keycloak OIDC (JWKS, `iss`/`aud`/`azp`), pairwise `oidcSub`, mints CIT session; `getSessionToken()` accepts Bearer. Inert until `KEYCLOAK_ISSUER`/`KEYCLOAK_CLIENT_ID` set. Guard/routes/middleware unchanged.
  - ✅ **Branch reconciliation done** — a divergent duplicate (`feat/oidc-resource-server`, `keycloakSub`) was salvage-checked and deleted. It lacked the OIDC tests **and** the `azp` sibling-app-token rejection + clock-tolerance the canonical branch has, so nothing to cherry-pick (optional tiny follow-up: canonical could also read the `name` claim to seed `User.name`). `feat/oidc-session-endpoint` is the single canonical OIDC branch.
  - ✅ Rate-limiting / revocation / timing-equalized login preserved (OIDC-only accounts guarded in login + delete)
  - ⬜ Retire the password login path once Keycloak is live; wire the OIDC **step-up** for delete/export/regimen
  - ⬜ Test end-to-end against the local dev Keycloak ([identity/dev/](identity/dev/))
- **Phase 3 — Rebuild CIT in Expo** 🟡 **underway** (native app `bas-apps/apps/cit`, ships as **Baseline**; remote **is** `Beaudoin0zach/bas-apps` — the "no remote" note here was stale, contradicting §6 and disproved by a push on 2026-07-18)
  - 🟡 Rebuild 7 screens + 3 auth flows in RN — app builds, OIDC hosted-login against prod Keycloak, sign-in hardening landed (watchdog, idToken re-prompt). Not all CIT web features ported (e.g. **no AI-insights screen yet**)
  - ⬜ Re-run a11y gates to parity (VoiceOver + TalkBack)
  - ⬜ i18n reusing CIT `locales/*.json`
  - ⬜ In-app account deletion (Apple 5.1.1(v))
- **Phase 4 — Ship to testers** 🟡 **on TestFlight now** (§2b) — the reality this roadmap once marked "not started"
  - 🟢 EAS Build → **TestFlight** — Baseline (CIT native) + the Access Atlas / KindredAccess webview wrappers are all on TestFlight; EAS Update (OTA) channels configured. ⬜ Play internal · ⬜ web
  - ⬜ Privacy nutrition labels / data-safety form
  - ⬜ Human-reviewed store copy (incl. Spanish)
  - ⬜ External TestFlight for Access Atlas blocked on Guideline 4.2 (needs camera evidence feature)
- **Phase 5 — Generalize** ⬜
  - 🟡 **KindredAccess OIDC RP done ahead of sequence** (2026-07-08) — Django resource server integrated with `mozilla-django-oidc` (confidential client + PKCE S256), verified end-to-end against the local dev Keycloak incl. a genuinely pairwise `sub`. Layered session (validate vs JWKS → mint Django session), verified-email linking (ADR-004), `azp` sibling-app rejection. Inert until `KEYCLOAK_ISSUER`/`OIDC_RP_CLIENT_ID` set. Branch `feat/bas-keycloak-oidc` (KA PR #4), 346 tests green. Existing-user migration + prod still pending — see §6.
  - ⬜ KindredAccess consumes shared packages (`ui`/`auth`)
  - ⬜ "Add a new app" playbook

---

## 4. Identity service (Keycloak) — [ADR-001](docs/adr/001-platform-architecture-and-identity.md)

Setup & hardening steps live in **[docs/keycloak-setup-and-hardening.md](docs/keycloak-setup-and-hardening.md)** (drafted, not yet executed).

- ✅ Decision: standalone, self-hosted Keycloak
- ✅ Stand-up + hardening checklist drafted ([keycloak-setup-and-hardening.md](docs/keycloak-setup-and-hardening.md))
- 🟢 **Instance stood up in PROD** — `https://id.beauaccesssolutions.com/realms/bas` is the live OIDC issuer (**migrated 2026-07-17** off `id.kindredaccess.org`, so the shared IdP no longer sits under one member app's domain). All five apps verified against it. The old host stays dual-served until the three native builds ship; then it 301s and its DNS record is dropped.
- ⬜ Hardening executed (admin-console lockdown, patching cadence) — confirm against the checklist now that it's live
- 🟡 Login theme re-themed to pass WCAG 2.2 AA — theme on `feat/identity-a11y-login-theme` (unmerged). ✅ **`/bas-design-review` done (2026-07-14):** passes the static/visual bar — contrast recomputed for all pairs in **both** light+dark (lowest 4.62:1 border / 6.2:1 dark button, all clear), focus/reflow/target-size/reduced-motion handled. Applied 2 fixes (checkbox 20→24px SC 2.5.8; input `font-size: max(1rem,16px)` iOS-zoom guarantee). ⬜ **One open verification:** it's CSS-only, so the login-**error** path (announce `role=alert` + move focus + preserve username) is inherited from `keycloak.v2` and must be exercised on the live page — may need a minimal template touch. ⬜ confirm this theme is the one serving on prod (`id.beauaccesssolutions.com`)
- 🟢 OIDC clients per app + `aud`/`azp` isolation — all five registered in the `bas` realm and exercised against **prod** (`id.beauaccesssolutions.com`): `cit-web`, `kindredaccess-web`, `benefits-navigator-web`, `disability-wiki-web`, and **`access-atlas`** (bare — the client IDs are *not* uniformly suffixed, so verify rather than infer). KA rejects a sibling `azp`.
- 🟡 Pairwise subject identifiers per client ([ADR-003](docs/adr/003-pairwise-subject-identifiers.md)) — **dev:** working for `kindredaccess-web` (`oidc-sha256-pairwise-sub-mapper`, salted; sub ≠ raw user id, verified). Reference bootstrap corrected for both clients (was a no-op `oidc-sub-mapper`). ⬜ prod sector-identifier/salt strategy
- ⬜ 2FA + step-up (ACR/LoA) policy
- ⬜ DR: Keycloak DB backup/restore + token signing-key rotation + availability target
- ⬜ Existing-user migration runbook — CIT first ([ADR-004](docs/adr/004-existing-user-migration.md))

---

## 5. App Store / Play prerequisites (CIT first)

> **Note:** all three apps are already on **TestFlight** (§2b), so the Apple side is further along than this list implied. CIT ships under the name **Baseline**.

- 🟢 Apple Developer Program — **enrolled** (required for the TestFlight builds now live); `ascAppId` wired into `bas-apps/apps/cit` submit profile
- ⬜ Google Play Developer — **$25 one-time** (no Android build yet)
- ⬜ Published privacy policy URL (CIT has a `/privacy` route — publish it)
- ✅ In-app account deletion reachable (Apple 5.1.1(v)) — CIT: `POST /api/auth/delete-account` (password + typed `DELETE`) wired to the Settings danger zone; real cascade + on-disk export purge
- ⬜ App Privacy nutrition labels / Play data-safety form
- ⬜ No medical/diagnostic claims in copy or AI output (CIT non-negotiable #4)
- ⬜ App name availability check (both stores)

---

## 6. Open items / blockers

- ✅ **Accessibility invariants moved from prose to CI gates (2026-07-18).** A verification sweep over the lessons files found four §4-spine entries that were *implemented correctly but untested* — the fix had landed and the reasoning sat in a code comment, which documents intent but does not defend it. Now enforced, all mutation-tested (the contract was broken and the right test confirmed to fail):
  - **page-repair** ([#1](https://github.com/Beaudoin0zach/page-repair/pull/1), merged) — live-region spine (both regions pre-created *before* the first message; failures assertive; partial progress polite) + `test/contrast.mjs` recomputing every text/UI pair from the token hexes **in both themes**, fail-closed on a `:root` token no verified pair claims. **This repo had no CI at all** — added `.github/workflows/ci.yml`.
  - **disability-wiki** ([#44](https://github.com/Beaudoin0zach/disability-wiki/pull/44), merged) — `tools/check-sw.mjs` **executes** the generated `sw.js` and asserts canonical-only precache + the redirect rewrap. The build already *ran* the generator and asserted nothing; running a generator is not a test. Blocking CI step.
  - **Benefits Navigator** ([#31](https://github.com/Beaudoin0zach/benefits_navigator/pull/31), merged via admin override) — the assistant's inline template script became `static/js/assistant.js` so it could be tested; `tests/js/assistant.a11y.test.mjs` pumps 200 deltas and asserts via MutationObserver that the polite region never changes, plus focus lands on the answer (done) / recovery control (error). Wired in as `npm run test:js`. ⚠️ **Note:** BN CI runs `pytest --ignore=tests/e2e`, so an a11y test placed in `tests/e2e` would gate nothing — that is why this went in as a Node suite. Extraction also removes one inline `<script>` (CSP `'unsafe-inline'` progress). **Unreviewed by a human** (admin bypass); the extraction's faithfulness rests on one reading.
  - **Scoping rule adopted:** a repo-local test does **not** graduate a cross-project lesson — it closes that repo's hole while every other surface keeps the trap. Entries are marked "(page-repair only)" / "(BN only)"; `packages/ui`, the Keycloak theme, and KindredAccess's chat surface remain unenforced prose.

- 🔴 **CIT native announced status to neither screen reader (2026-07-18)** — `bas-apps/apps/cit` had 14 status messages as `{error && <Text accessibilityRole="alert">}`. That role marks a trait but does **not** announce dynamic changes; announcing needs `accessibilityLiveRegion`, which is Android-only and used nowhere in the repo. So every one was silent to **VoiceOver and TalkBack both**, in the app shipping as Baseline on TestFlight. Two of the 14 were a second defect — a *success* message routed to the assertive channel (§4 C1). **Fix open as [bas-apps#1](https://github.com/Beaudoin0zach/bas-apps/pull/1)**: adds the `packages/ui` live-region utility that design-principles §4 always claimed `packages/ui` provided and which **did not exist** — the root cause of every app hand-rolling status. Ships `bas-apps`' first tests and first CI (both gates green). ⚠️ **Not device-verified** — RN semantics and code confirmed, no VoiceOver/TalkBack run on hardware; that check should precede merge.

- ⚠️ **`bas-apps` is invisible to platform tooling** — it has no symlink in `repos/`, so `platform-status.sh` does not report it and the §4.1 audit initially missed it. Everything else the platform ships is registered there. Adding it would close the gap.

- ✅ **§4.1 gate audit (2026-07-18)** — C3 (no double-read) gained a real gate: page-repair `test/unit.mjs` "no double-read (§4 C3)", merged as [page-repair#2](https://github.com/Beaudoin0zach/page-repair/pull/2), CI green. Auditing the other rows found the table wrong in both directions: C1 was credited to page-repair alone (BN enforces it more completely; KindredAccess only the markup half), C3 was marked "unenforced everywhere" when KindredAccess had shipped a gate with the original fix, and CIT web was written off as "not checked out" when it is registered in `repos/` and carries the portfolio's **strongest** C4 gate. Partial gates are now marked as such — a named gate had been reading as full coverage when it usually covers one half.

- 🔴 **CIT shipped nothing to production for 5 days and no signal said so (2026-07-14 → 07-19)** — `npm ci` lockfile drift (`Missing: @swc/helpers@0.5.23`) failed at the *install* step, which is shared by CI and the DO build. Consequences, both silent: **CI never reached a test** (it died in 12s, so PRs #40 and #41 both merged over red runs), and **three consecutive deploys ERRORed** (07-14, 07-16, 07-19) while DO kept serving the 2026-07-14 container and `/api/health` returned 200 throughout. This is the DO trap in the skill — a failed deploy rolls back to the OLD container and "Healthy" only reflects the health-check path — but the *new* lesson is that a green-looking repo and a 200 can coexist with a five-day production freeze. Fixed by [#42](https://github.com/Beaudoin0zach/Chronic-Illness-Tracker/pull/42): lockfile regenerated in a linux `node:22` container (npm 10.9.8, per the macOS/npm-11-breaks-DO trap), 7 added transitive entries, no removals or version changes. CI green in 1m27s (vs 12s failing); deploy reached **ACTIVE 10/10**. ⚠️ **Open gap:** nothing alerts on deploy failure — three failures over five days raised zero notification, and the health check actively masked it. Applies to every `deploy_on_push` app on the platform, not just CIT.
- ✅ **CIT numeric-PHI leak found in passing (2026-07-18, MERGED + LIVE 2026-07-19)** — CIT's PHI drift test parses `schema.prisma` but collected only `String` columns, so `bpSystolic` / `bpDiastolic` / `heartRate` / `temperature` were unclassified in `PHI_FIELDS`. **Merged as [Chronic-Illness-Tracker#41](https://github.com/Beaudoin0zach/Chronic-Illness-Tracker/pull/41)** (`c2f50d4`); reached production only after #42 unblocked the deploy path. Parser now covers all scalar types **plus enum columns**; widening it alone surfaced **390 unclassified columns**. ⚠️ **Mechanism correction:** the exposure path is `sanitize()`'s structured-payload branch (a strict allow-list), **not** stringified errors — `scrubString` already default-redacts every `key: value` outside a small diagnostic allow-list, so the earlier "passed unredacted into stringified Prisma errors" claim here was wrong. **`status` resolved as PHI** (2026-07-18, reversing the initial "safe" call): a `DoseEvent.status` beside a `userId` is adherence data on a named user, which non-negotiable #9 exists to prevent. Redacted in *both* scrub paths — removing it from `scrubString`'s `SAFE_VALUE_KEYS` was the load-bearing half, since leaving it there would have kept `status: SKIPPED` printable inside stringified Prisma errors. Accepted cost: export/HTTP status share the name and are now redacted too; log `statusCode` (still safe in both paths). Remaining reviewer call: all `DateTime` columns are classified safe. General lesson recorded: *a derived test still has the shape of its parser — check what your parser cannot see.*

- 🟡 **`/bas-design-review` sweep (2026-07-14).** Reviewed to the WCAG 2.2 AA bar. ✅ **Done:** Benefits Navigator, CIT, **Keycloak login theme** (passed static layer; 2 CSS fixes applied — 24px checkbox + guaranteed 16px input; open: verify the inherited `keycloak.v2` error path announces/moves focus), **KindredAccess** (non-chat surfaces — found + fixed a **dark-theme contrast blocker** on the signup password meter/requirements, plus autocomplete/dark-theme/live-region should-fixes; ✅ **merged to `main`** — ships on next manual deploy), **Access Atlas** (passed; fixed the **form-input-loss on validation** via a zero-JS cookie echo + field-level errors + up-front size limit; ✅ **merged to `main`** → auto-deployed to prod), **page-repair** (blocking finding = no assertive `role="alert"` region for genuine failures; turned out the fix already existed on the stranded 07-13 `a11y/overlay-ux-doc-and-review-fixes` branch — **merged to `Beaudoin0zach/main`** along with options dark-theme + shadow-root isolation, plus closed the last two items: 24px reveal checkbox + blame-free timeout copy; 22 tests pass), **a11y-probe** (2 hard AA failures — `user-scalable=no` disabled zoom + a 3.56:1 dark Start button — plus input-border contrast + a per-keystroke live region; all fixed, ✅ **merged to `main`**, pushed), **marketing site** (light-only static; one AA miss — footer `text-gray-500` ~3.6:1 → `gray-400` ~7:1 — plus a reduced-motion guard, contact-form autofill tokens, and deleting a dead Tailwind v4 config; fixed on branch `a11y/review-fixes`, ⬜ **not merged** — repo is local-only + has active WIP, so folded in by owner). **✅ SWEEP COMPLETE — all 8 surfaces reviewed to the AA bar.** Minor deferred: KA password-match input *border* color (`signup.html:406`) is theme-fixed hex (reinforced by text, so not color-alone). Product decisions left to owner (marketing site): Contact not in desktop header, "Get Started" has two destinations.
  - ⚠️ **page-repair repo consolidation:** `Beaudoin0zach/page-repair` is now the current/primary repo — **9 commits ahead** of the "canonical" `LangworthyWatch/page-repair` (last pushed 2026-07-07, a strict ancestor). Per the decision to have it live under Beaudoin0zach, treat Beaudoin0zach as canonical; LangworthyWatch is stale — sync or retire it.
- 🟡 **CIT launch-readiness sweep (2026-07-08)** — landed on branch `chore/launch-prep` (pushed to `Beaudoin0zach/Chronic-Illness-Tracker`, 2 commits; **PR not opened** — <https://github.com/Beaudoin0zach/Chronic-Illness-Tracker/compare/main...chore/launch-prep>). ⚠ Branched off `fix/security-audit-batch-4`, so it currently stacks on the unmerged security work.
  - ✅ **Full-app i18n** — closes non-negotiable #10 (was a hard launch gate). next-intl is now actually wired (plugin + provider + dynamic `lang`); all ~35 pages/components render from `locales/en.json` (~250 new keys). `RELEASED_LOCALES=['en']` structurally blocks the unreviewed `es.json` from ever being served (#11). Reuses `locales/*.json` cleanly for the eventual Expo rebuild (Phase 3).
  - ✅ **CI backstop** — `.github/workflows/ci.yml` runs lint + 188 tests + build on every push/PR to `main` (there was none). Doubles as the a11y/import-boundary gate seam for Phase 0.
  - ✅ **Change-password + "log out other devices"** — new endpoints wired into Settings; first real trigger for session revocation. (Password login is retired later once Keycloak is live — Phase 2.)
  - ✅ **macOS PHI pre-commit hook** — was a silent no-op (`grep -P` on BSD grep); reimplemented in python3.
  - ✅ **Security-audit batches 1–4 are now merged to `main`** (`fix/security-audit-batch-1..4`), so that gate is cleared. `chore/launch-prep` itself may still be unmerged. Still to do before a live deploy: **rotate the Anthropic API key** (real key in local `.env`), and no scheduled AI-retention job yet.
  - ✅ **Signup account-enumeration closed** — email-verification signup via Postmark (**CIT PR #2 merged to `main`**). Replaces the `409 email_taken` oracle with a uniform `202`; login now blocked until verified. ⚠ **Deploy gate:** the live app needs `EMAIL_PROVIDER=postmark` + `POSTMARK_API_TOKEN` + `EMAIL_FROM` set or **no one can sign in** (main has `deploy_on_push`). CI didn't gate this specific merge, but **CIT CI is now working and green on `main`** — the repo's Actions had never once run since the `Beaudoin0zach` migration; triggering them surfaced (and PR #7 fixed) a lint failure + a `jest` config that couldn't load on CI's Node 20. `main` now passes Lint/Test/Build.
- ✅ **Disability Wiki added to the tracker (2026-07-16)** — reconciled a **one-sided membership**: `Beaudoin0zach/disability-wiki` had declared itself a BAS member in its own `docs/platform-membership.md` (2026-07-14) but was absent from this tracker, README, and PLATFORM. Now listed in §1 + §2. It has **migrated off Wiki.js to static Astro Starlight** (~540 pages on Cloudflare Pages, live at <https://disabilitywiki.org>); the legacy Wiki.js `docker-compose.yml` on the default branch is retired scaffolding, not the live stack. Built **Access-Atlas-shaped**: account-free static browsing + Keycloak BFF contributor auth + Supabase moderation queue (Phases 1–3 code-complete + tested on a feature branch, fail-closed, inert until a Supabase project + registered Keycloak client + data-controller sign-off). **Key divergence from Atlas:** its read path is **DB-free static** (Supabase is write-only staging), so the two stay *sibling apps sharing a template*, not one merged app. Also fixed a stale governance URL in its membership doc (`Beau-Access-Solutions` → `bas-platform`).
- ✅ **access-directory (Access Atlas) now has a remote** — `Beaudoin0zach/access-atlas` (public), onboarded on `main` with a governance pointer + inlined invariants (`docs/platform-membership.md`). Scoped as a full identity member: browsing stays account-free; identity gates contribution only; browsing surface stays Astro/zero-JS (no RN rewrite).
- ✅ **Access Atlas app-side invariants — 3 of 5 merged to `main`** (PR #1, `platform-seed-and-data-rights`; also landed the WNY seed pipeline + drop-in Keycloak BFF contributor auth):
  - ✅ **#2 tracking/CSP** — own CSP + security headers (one policy, applied as `<meta>` for static pages + HTTP headers for SSR); `script-src 'none'` makes its zero-JS surface self-enforcing.
  - ✅ **#3 decoupled delete/export** — complete, independently-callable workflow (`src/lib/data-rights.ts` + ops CLI, storage-aware, idempotent, unit-tested), keyed by contributor id so the Keycloak `sub` drops in unchanged. Self-service UI door deferred to the authenticated contribute milestone.
  - ✅ **#4 contribution boundary** — `.github/CODEOWNERS` on the write path, service-role client, identity seam, and safety-critical SQL (needs "Require review from Code Owners" toggled on in branch protection).
  - 🟡 **#1 layered sessions** now underway — a **drop-in Keycloak contributor auth (server-side BFF)** landed on the branch. ⏳ **#5 i18n** still pending Keycloak (Phase 0/1). Also on the branch: a WNY seed-data importer (creates self-reported data only).
- ✅ **Mobile app source backup — COMPLETE (2026-07-14).** All four mobile repos pushed to private `Beaudoin0zach/*` repos: **`bas-apps`** (the **Baseline**/CIT native Expo app + shared `ui`/`auth`/`tokens`/`i18n`/`api` packages), **`kindredaccess-ios`** (KA wrapper, incl. its App Store screenshots), **`bas-frontend`**, and **`access-atlas-mobile`** (its uncommitted EAS/App.js work was committed first as a backup snapshot). The Access Atlas + KindredAccess *web* wrapper configs already live inside their pushed web repos.**Decision (2026-07-14, reviewed + EXECUTED): retired `access-atlas-mobile`** (redundant Expo webview wrapper) — GitHub repo **archived** (read-only, still backed up). Canonical Access Atlas iOS build stays the `access-directory` Capacitor wrapper (Capacitor = platform wrapper toolchain; Expo = the native CIT app). ✅ **Salvaged its one good idea:** the Expo `App.js` kept the Keycloak IdP host in-app; the Capacitor config had **no `allowNavigation`**, so its default would bounce OIDC login to Safari and break the round-trip. Fix (`allowNavigation: ['id.kindredaccess.org']`; everything else opens in Safari) **merged to `access-directory` `main`** (2026-07-14) — ⚠️ *superseded 2026-07-17: that host is now `id.beauaccesssolutions.com` (branch `feat/idp-beauaccesssolutions-allownav`, pending a rebuild); don't copy the old value* and the local Expo dir **deleted** (remote archived + backed up). ✅ **New TestFlight build uploaded** (2026-07-14, build 2 via `cap sync ios` → Xcode archive) carrying the fix. ⬜ Final check: once it finishes processing, install build 2 and confirm login opens the Keycloak screen **in-app** (not Safari) and returns logged in.
- ✅ **a11y-probe repo initialized** — `Beaudoin0zach/a11y-probe` (private), `main` pushed (initial commit 2026-07-14). ⬜ still not published to Devvit.
  - ✅ **Real README + descriptive post title (2026-07-19)** — branch `docs/real-readme` pushed, ⬜ **no PR yet**. The style-eval pass found a11y-probe was the *only* portfolio app with zero scorable prose: the README was unmodified Devvit React Starter boilerplate that still told the reader to run `npm create devvit@latest` to create the project that already existed, and never mentioned accessibility. Replaced with what the app does — **Test 0** on the inline splash surface (`splash.tsx`) plus **six tests** in the expanded webview (headings/landmarks, polite live region, assertive announcement, programmatic focus, text input, alt text). Post title went from the bare string `a11y-probe` to a sentence (`post.ts:5`) — it's the first string a screen reader hits. ⚠️ **Three claims in the handoff draft were wrong against source and were corrected before writing:** the webview is only an *iframe* on desktop web (native WebView in the Reddit mobile apps); the sections are not "static" (tests 2–5 are interactive); and the draft missed Test 0 entirely. ⬜ Noted, not fixed: `devvit.json` still registers the starter's "Example form" menu item + `onAppInstall` trigger, and `src/client/hooks/useCounter.ts` looks unreferenced.
- ✅ **Pointer PRs merged** — CIT #1, KindredAccess #2, Benefits Navigator #23, and Access Atlas #1 are all **merged to `main`**; page-repair's remains unverifiable from this account (third-party `LangworthyWatch` repo).
- ✅ **Push governance repo** — done (`main` live).
- 🟡 **KindredAccess OIDC integration** (2026-07-08) — Django resource server done and verified end-to-end vs dev Keycloak (branch `feat/bas-keycloak-oidc`, KA PR #4). Stores a pairwise `sub` on a new `KeycloakIdentity` model; inert until configured. While verifying, **fixed the dev-realm pairwise mapper** in `identity/dev/realm/bootstrap.sh` for **both** `cit-web` and `kindredaccess-web` — the reference used `oidc-sub-mapper` (non-pairwise, sub = raw user id) instead of `oidc-sha256-pairwise-sub-mapper`. Separately, KA's WebSocket deploy config was corrected (Gunicorn+Daphne, KA PR #3). ⬜ Existing-user migration for KA still pending (below).
- 🟡 **Cross-app correlation** — adopt pairwise `sub` ([ADR-003](docs/adr/003-pairwise-subject-identifiers.md)) before any app stores a shared identifier. **KA now stores a pairwise sub (verified in dev).** ⬜ enforce for `cit-web` and in prod (needs sector-identifier/salt strategy).
- 🟡 **Existing-user migration** into Keycloak ([ADR-004](docs/adr/004-existing-user-migration.md)) — CIT reference runbook, then KA + Benefits Navigator. (KA code links legacy accounts by verified email at first login.) KA's Keycloak user-migration **export command** (ADR-004) is now **merged to `main`** (KA PR #5, which also landed an automated vision first-pass for photo moderation). **Hardened via adversarially-verified review (KA PR #6, merged 2026-07-09):** removed the superuser `emailVerified:true` carve-out (unverified admin emails were an IdP-password-reset takeover vector for the highest-privilege accounts — unverified admins now take the one-time reset path), plus moderation-pipeline fixes. The **firstName/lastName-optional user profile** KA migration needs is already covered: `main`'s bootstrap (platform PR #4, validated on KC 26) relaxes the declarative profile — Keycloak 24+ otherwise flags KA's single-display_name migrated accounts "not fully set up" and blocks login. Remaining: partialImport dry-run + one migrated-login round-trip on the rebuilt realm, then the prod runbook.
- 🟡 **Benefits Navigator data posture** — framed in [ADR-005](docs/adr/005-benefits-navigator-data-posture.md); BN is engineered conservatively to full-Member spec, but stays **Candidate** pending a **BAS LLC legal determination** (38 U.S.C. §5701 / 38 CFR §§1.500–1.527 / Privacy Act flow-down vs HIPAA). This is the one item gating Candidate → Member — a decision, not code.
- ✅ **BN staging secrets + DB hardening — DONE (2026-07-18).** On DO app `benefits-navigator-staging` (serves **vabenefitsnavigator.org**): (1) `SECRET_KEY` / `FIELD_ENCRYPTION_KEY` / `OIDC_RP_CLIENT_SECRET` converted to **`type: SECRET`** — no longer readable via `doctl apps spec get`; the `benefits-navigator-web` **client secret was also rotated in Keycloak** first (the old value had been plaintext-readable in the spec), the new one persisted to `/opt/keycloak/secrets.env` — closing the long-standing "generated but never persisted" gap — and verified at the **token endpoint** (rotated secret + bogus code → `invalid_grant`; wrong secret → `unauthorized_client`); (2) all **component-level duplicate env vars removed**, leaving one app-level definition each (component copies silently override app-level ones — the trap documented in the `do-app-platform-debug` skill); (3) `DATABASE_URL` / `REDIS_URL` are now **bindable references** (`${benefits-nav-db.DATABASE_URL}`, `${valkey.DATABASE_URL}`) with both managed clusters attached as spec `databases:` components, so DO injects current credentials at deploy time and a password rotation can't strand the app; (4) the PG binding moved **off `doadmin` to a dedicated `benefits_navigator` cluster user** that now owns the DB and all 84 tables (`doadmin` retains access via role membership for manual ops). **This is the pattern to copy to Access Atlas and CIT** — both still hold plaintext env vars. ⬜ Judgment call still open: whether to **rotate** `SECRET_KEY`/`FIELD_ENCRYPTION_KEY` (they sat plaintext-readable in the spec for a long time; rotation drops sessions / breaks encrypted fields).
- ✅ **BN staging Redis outage — RESOLVED (2026-07-18).** Login had been 500ing while `/` returned 200: the **web** component carried a `REDIS_URL` override pointing at an **Upstash** instance whose 500k free-tier quota was exhausted (`max requests limit exceeded` on every session write), while worker/migrate used the healthy DO Valkey — so web and worker were also on **different Celery brokers**. Removing the override (part of the hardening above) put every component on `db-valkey-nyc3-98037`. Verified end-to-end on the prod domain. The orphaned **Upstash account has been deleted** (2026-07-18); confirmed zero references remained in any DO app spec, the `bas-keycloak` droplet, or any repo, and all three DO apps probe healthy after deletion.
- 🟡 **page-repair store submission prepared** — v1.0.0 release manifest, icons, [PRIVACY.md](repos/page-repair/PRIVACY.md), [STORE_LISTING.md](repos/page-repair/STORE_LISTING.md), and `dist/page-repair.zip` are ready; **not submitted** (needs a Chrome Web Store dev account + real-page screenshots). Changes are uncommitted in the working tree.
- ⏳ **page-repair proxy inert** — Cloudflare Worker is live but needs `wrangler secret put ANTHROPIC_API_KEY` (+ a redeploy of the pending health-route change) before paid labeling works.
- 🟢 **Marketing site — LIVE at <https://beauaccesssolutions.com>** (2026-07-16). Repo **`Beaudoin0zach/bas-website`** (private); the site was **remade around the app portfolio** (new `/apps` page featuring Baseline/KindredAccess/BN/Access Atlas/Page Repair/a11y-probe, home repositioned to lead with products, Contact added to nav) while **keeping the consulting content**. Deployed via `netlify deploy --prod` from the site dir (Netlify site `620702da`, `beaudoin0zach@gmail.com` account). Cleaned up: removed the SPA `/* → /index.html` catch-all + added a branded 404 page (mistyped URLs now 404 instead of silently showing home). ✅ **Continuous deploy now wired** — Netlify site linked to `Beaudoin0zach/bas-website` `main` (`npm run build` → `dist`); verified a push auto-builds + publishes. ⚠️ **Incident + naming hazard (2026-07-16):** the repo was first mis-linked in the Netlify UI to **`Beaudoin0zach/Beau-Access-Solutions`** (this platform hub — near-identical name), so a TRACKER push to *this* repo triggered a wrong-repo build that broke the live site (home → 404); restored by a manual redeploy and the link corrected to `bas-website`. ✅ **Resolved by renaming this platform repo `Beau-Access-Solutions` → `Beaudoin0zach/bas-platform` (2026-07-16)** so it can't be confused with the `bas-website` site repo again (GitHub auto-redirects the old name; local remote repointed). The marketing Netlify site watches `bas-website` only, so pushing here is safe.
  - ✅ **App links + a11y + CI pass (2026-07-18, 3 commits, live).** (1) **Disability Wiki was missing from the site entirely** despite being live — added, and VA Benefits Navigator repointed from `/contact` to <https://vabenefitsnavigator.org>; both probed 200 before linking. Atlas + CIT still show DO hostnames (no custom domain yet), which is now the only unpolished link on `/apps`. (2) Site settled on **company "we"** — it had been mixed (`/apps` all "we", `/services`+`/about` all "I", home both on one page); first person preserved where it's the only truthful option (the `/about` bio, and the contact form's "I'm interested in…" label, where the "I" is the *visitor*). (3) **a11y:** four design tokens (`bg-forest-50`, `text-forest-800/900`, `border-forest-200`) were used but never defined, so Tailwind emitted **zero CSS** and the `/apps` status chips rendered unstyled — silently, since dark-on-light still looks plausible; plus 4 computed contrast failures (terracotta chip 3.00:1, required asterisk 3.76:1, input borders 1.47:1, and blog body text on a *tinted* background 3.93:1 — that last one only appears once the background's alpha is blended, which a white-only sweep cannot see); `aria-current` and the visual nav highlight used different predicates, so on `/apps/access-atlas` sighted users saw "Apps" lit and screen-reader users were told nothing was current. (4) **This repo had no CI and no tests** — added `test/contrast.mjs` (18 pairs recomputed from token hexes with alpha blending, **plus an undefined-token scanner**) wired into `npm run build`, so Netlify — whose build command *is* `npm run build` — cannot publish a regression; verified fail-closed by injecting both regression types. (5) Fonts **self-hosted** (Google now ships these as latin *variable* files: 2 files/82 KB, not 5 statics) — page now makes **zero third-party requests**, verified via `performance.getEntriesByType('resource')`. ⬜ Still open: a founder note on the home page (drafted, awaiting the owner's own words).
  - ✅ **Contact form + real dark mode (2026-07-19, 2 commits, live).** (1) **Contact form** rebuilt as progressive enhancement: `action="/thanks"` + a real branded page so the no-JS path stops landing on Netlify's generic screen, and `novalidate` is applied **by the script, never in the markup** (putting it in HTML would strip no-JS users of validation entirely). Spine-compliant: both live regions pre-created (C1), and field errors are wired via `aria-describedby` rather than announced — focusing the field reads label+problem once, where a live region would read every error twice (C3); the assertive region is reserved for submit failure, which has no field to focus. Validation on blur, never mid-first-entry. Driven in a browser across all four paths; confirmed the POST carries `form-name=contact`, **without which Netlify silently rejects the submission**. (2) **Real dark mode** — the blocker was never the palette: **~47% of the site's colour was raw Tailwind** (`text-gray-900`/`bg-white`, 181 usages) which cannot flip, so brand-token-only dark would have left half the page light. Added a semantic layer (canvas/surface/raised/ink/body/muted/line/field-line + accent/on-accent + dedicated triplets for the deliberately-dark footer, logo mark and CTA band) and migrated all 181. Warm, not inverted (#1a1817, warm off-white text — pure white on near-black haloes for astigmatic/dyslexic readers); every green lightens (sage is 2.4:1 on the dark canvas). The gate now runs **36 pairs × 2 themes** with per-theme alpha compositing, plus a coverage check that fails if any semantic token has no dark value. ⚠️ **It caught a pre-existing LIGHT-theme failure nobody had found:** the logo's white "B" on `#5d8a61` has been at **3.98:1 on the live site all along** — it survived the full design review and two contrast passes, and only surfaced because building dark mode forced every pair through the math. *General lesson: a contrast sweep only checks the pairs someone remembered to list; adding a second theme is what audits the list itself.*
  - ✅ **Caching, perf, and a prose gate (2026-07-19, 2 commits, live).** (1) **Repeat visits were paying for nothing:** `netlify.toml` set security headers but no caching, so Netlify's `max-age=0,must-revalidate` default applied even to content-hashed assets — a revalidation round-trip on all four subresources, 87% of page weight being the two fonts. `/_astro/*` is Astro-hashed so it went `immutable`; `/fonts/*` was **not** hashed (we named those files), so the filenames were given a content hash first rather than shipping a footgun where a later font swap serves stale bytes for a year. Measured before the change: TTFB 136ms cold / 28ms warm, CLS 0, 4 requests, 93 KB, **zero third-party**. ✅ **`web-perf` since run for real** (chrome-devtools MCP installed as an official plugin, 2026-07-19): **LCP 288 ms desktop / 667 ms on throttled mobile (Slow 4G, 4× CPU), CLS 0.00 on both** — 3.7× inside the "good" threshold at its worst. Chrome estimates **zero savings** on every insight, so no perf work is warranted. The trace also *verified the caching fix on a live request* (`cache-control: public,max-age=31536000,immutable` on the font; HTML correctly 304s). Note the LCP element is **text**, not an image — which is why 89% of LCP is render delay rather than resource load, and why adding a hero image is the one change that could regress this page. ⚠️ Lab numbers only: the site has **no CrUX field data**, so these are one machine, not real users. (2) **`/style-eval` across all 13 pages** scored Voice 2.8 / Quality 3.6 and measured **10.7 em-dashes per 1,000 words** against the book style sheet's ≤4 — the density that sheet names as an AI-prose tell. Cut to **3.5** in ~34 edits (split sentences, never flatten voice) and gated: `test/prose.mjs` runs post-build on the **built HTML**. ⚠️ **The gate reads `dist/`, not `src/`, and that is the finding**: the first source-parsing version reported 8.9/1k when the truth was 10.7 (it stripped frontmatter, and `/apps` keeps its copy in a JS array there), and even the *corrected* parser still under-counted ~1/page. Recorded in shared LESSONS: *when a build produces the thing you want to check, check the thing it produced.* ⬜ Still open: place the founder note (drafted, needs the owner's words), and source the "1 in 4 adults" statistic.
- ⬜ Decide the shared-frontend repo name (`design-system`) when Phase 0 needs shared code.

---

## 7. Decision log

- [PLATFORM.md](PLATFORM.md) — architecture anchor
- [INVARIANTS.md](INVARIANTS.md) — the five platform invariants
- [ADR-001](docs/adr/001-platform-architecture-and-identity.md) — shared platform + standalone Keycloak identity
- [ADR-002](docs/adr/002-umbrella-org-and-repo-topology.md) — BAS umbrella, repo topology, no committed cross-repo symlinks
- [ADR-003](docs/adr/003-pairwise-subject-identifiers.md) — pairwise subject identifiers (no cross-app correlation)
- [ADR-004](docs/adr/004-existing-user-migration.md) — migrating existing users into Keycloak
- [ADR-005](docs/adr/005-benefits-navigator-data-posture.md) — Benefits Navigator data posture (Privacy Act / VA vs HIPAA)
- CIT `docs/adr/004` — CIT-side pointer to the identity decision
- CIT `docs/mobile/PLAN.md` — native build plan; `docs/mobile/auth-token-exchange.md` — token-exchange spec
- **Private:** `Beaudoin0zach/bas-internal` — business-sensitive + consolidated-security docs (pricing/cost model, review remediations) kept out of this public repo
